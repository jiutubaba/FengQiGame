# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-08-12 [Codex] 地图后台路由领域化代码治理

- 模块边界：`server/routes/maps.js` 从 2813 行业务混合文件收口为 31 行组合入口；原有实现按地图生命周期、玩家与消息、排行榜、风控、礼包与群抽、运营资源、文件、API Key 拆入 `server/routes/maps/` 的 9 个领域模块，仅将跨领域复用的 ID 校验与分页解析保留在 `shared.js`。没有新增依赖、兼容层、API 或业务行为。
- 合约保持：组合入口仍挂载于 `/api/maps`，各领域注册顺序与拆分前一致；对拆分前后 Express 路由清单逐项比较，61 条路由的方法、路径、顺序和中间件数量完全一致，模块入口可独立导入。权限中间件、事务范围、SQL、响应数据形状和审计动作均保留在原业务流程内。
- 文档治理：架构、源码上下文路由、代码组织规范和各领域主要证据已同步到新目录；自动事实生成器补齐对组合路由同名目录的递归扫描，生成事实仍包含 98 条 API，其中 61 条地图路由准确指向各领域文件；模块边界待办已完成。
- 回归修复：最终复核时，本地预览暴露出趋势数据缺少当前指标字段时 SVG 圆点纵坐标为 `NaN`；`MetricsPanel` 改为复用同一组已归一化数值计算纵坐标，保持缺失字段按既有口径归零。重新打开地图指标页后，8 个趋势节点坐标均为有限数值，控制台无警告或错误。
- 验证：`npm run check` 通过（18 个必需入口、23 份文档、13 项单元测试与生产构建），`npm run audit:prod` 为 0，`git diff --check` 通过。隔离集成测试在执行用例前因本机环境阻塞：仓库 `.env` 为 Docker 模式但 Docker 引擎未启动，本机 PostgreSQL 使用不同凭据，连接 `127.0.0.1:55432/fengqi_test` 认证失败，17 项用例未执行；未重置数据库凭据、未连接正式库、未发布正式环境。本地指标页预览已按前端交付规则打开并保留。

## 2026-08-12 [Codex] 后台前端信息架构、恢复能力与按需加载优化

- 信息架构与视觉：保留既有暖白、灰阶和单一蓝色的运营控制台基线；地图功能导航按“数据监控 / 玩家运营 / 地图管理”分组，数据中心按核心经营、用户流动、留存复玩重排指标层级，并提高正文、表格、说明与操作控件的最小字号。地图卡片改为语义化链接，窄屏登录页恢复返回首页入口，未增加装饰性模块或改变业务含义。
- 交互与可访问性：路由和地图功能写入动态页面标题；移动抽屉增加滚动锁定、背景 `inert`、初始焦点、Tab 焦点循环、Escape 关闭与焦点归还；账户菜单补齐外部点击、Escape、上下方向键及 Home/End。地图中心、排行榜和礼包关键视图/搜索/选择状态写入 URL，刷新或分享链接可恢复；搜索框、视图切换与地图入口补齐可访问名称和状态语义。
- 数据恢复与性能：地图中心及地图工作台各业务域的首屏失败显示可重试错误，刷新失败则保留上次成功数据并显示持久提示。`src/pages/MapWorkspace.jsx` 从 4832 行收口为 185 行壳层，指标、配置、玩家、排行榜、风控、礼包与资源面板拆入 `src/pages/map-workspace/` 并按当前功能懒加载；所有页面路由改为懒加载，移除未再使用的 `Noto Serif SC` 依赖。未改变 API、权限、事务或服务端校验。
- 前端交付规则：`.ai/quality.md` 明确要求所有涉及 `src/**` 页面、组件或样式的任务在代码与检查完成后启动本地预览、打开对应页面并保留浏览器标签页；真实 API 不可用而使用合成数据时必须标注，不能用构建通过代替已打开预览。字体规则同步为当前实际使用的单一 `Noto Sans SC Variable`。
- 验证：`npm run check` 通过（18 个必需入口、23 份文档、13 项单元测试与生产构建），`npm run audit:prod` 为 0。本地浏览器使用合成数据完成桌面与 390px 窄屏验收，覆盖地图中心、指标分组、排行榜与礼包 URL 恢复、移动抽屉和账户菜单键盘焦点、刷新失败保留旧数据及登录页返回入口，浏览器控制台无警告或异常。本机真实 API 因当前数据库主机配置为容器名 `db` 而无法启动（`ENOTFOUND db`），因此未连接真实 PostgreSQL、未运行隔离集成测试、未发布正式环境。

## 2026-08-09 [Codex] 前端错误恢复、提交状态与危险操作体验统一

- 通用交互：新增可重试错误状态、页面内提示和统一确认弹窗；模态框补齐初始焦点、Tab 焦点循环、Escape、背景关闭控制、滚动锁定与关闭后焦点归还，表单字段支持就地错误说明。
- 页面接入：登录、地图中心、审计、账号与权限、系统设置、个人资料和地图工作台在加载或保存失败时保留原位反馈；设置、资料、账号、权限和地图配置补齐保存中状态、防重复提交与未保存提示。地图归档、玩家删除、排行榜发布或删除、风控规则、礼包、群抽、主播点位、文件和 API Key 等危险操作统一为站内确认体验，未改变既有权限、接口参数或服务端校验。
- 导航与数据操作：登录后顶栏补齐地图和当前功能面包屑，“地图中心”和当前地图均可点击直达，当前页保留不可点击的位置语义；390px 窄屏新增地图功能横向快速切换。玩家、风控、排行榜与账号列表统一展示当前筛选摘要，玩家、风控和账号关键筛选写入 URL 并支持刷新恢复。窄屏数据表增加横向滑动提示并固定关键操作列，未改变接口、权限或业务状态语义。
- 排行榜快捷封禁：实时榜条目在“移除”后新增“拉黑”，排行榜管理者确认后按地图和 UID 写入玩家 `rank_ban=true` 并记录 `leaderboard.player.ban` 审计；该玩家从当前地图所有实时榜和后续发布中排除，候选条目、每日采集事实与已发布历史快照保持不变，可在玩家管理取消排行榜封禁。排行榜操作列在桌面和窄屏均固定于右侧。
- 验证：`npm run check` 通过（18 个必需入口、23 份文档、13 项单元测试与生产构建），`npm run audit:prod` 为 0；Vite 与 React 插件按实际用途归入构建期开发依赖，不进入正式运行镜像。临时隔离 PostgreSQL 全链路 17/17 通过，覆盖快捷封禁的管理权限、玩家状态、实时榜排除、历史快照不可变与审计。本地浏览器完成登录页及登录后页面的桌面与 390px 窄屏验收，覆盖导航切换、面包屑两级直达、筛选 URL 持久化、排行榜“移除 / 拉黑”操作列和确认弹窗，控制台无页面运行异常。PR #30 已合并为 `main@ce7cec1`，`main` CI 运行 `31273132058` 的质量/审计与隔离 PostgreSQL 两项 required checks 均成功。
- 生产发布：GitHub 非正式发布 `deploy-ce7cec1` 的源码包与正式构建包完成 SHA-256 校验；发布前数据库备份 `fengqi-20260809-030040.dump` 与上传卷备份 `fengqi-uploads-20260809-030041.tar.gz` 均完成私有 OSS CRC64 校验。首次构建因 `.dockerignore` 排除 `dist` 在源码同步前失败，自动回滚后旧应用保持 healthy；随后使用独立最小上下文成功执行 app-only 发布。正式 `.release-commit` 为 `ce7cec1`，镜像为 `sha256:0f6430ddad7b9dbba67f1c927daef72bc9889d416b93a9c29bc1ad463f8d01e5`，旧镜像标签 `pre-bc344155-20260809-030317` 与源码备份 `source-pre-bc344155-20260809-030317.tar.gz` 已保留。迁移元数据、排行榜封禁链路、三个容器、备份 timer、公网状态、安全头、缺 Key 401 和新前端资源均通过只读验收；未改数据库业务数据、上传卷、Caddy、`.env` 或网络配置。

## 2026-08-08 [Codex] 正式服务器只读 S 盘映射

- 访问边界：只为本机当前真实公网 IPv4 在正式 ECS 安全组和 UFW 增加 TCP 22 的单地址 `/32` 规则；网页代理出口的误判规则已在验证后从两层防火墙撤销。`sshd`、SFTP 子系统、主机指纹和现有 `ecs-user` 身份文件均已通过真实连接验证，没有开放网段、密码登录、root 直登或数据库端口。
- 本机挂载：安装 WinFsp 2.1.25156 与 rclone 1.75.0，通过 SFTP 将正式 `/opt/fengqigame` 映射为网络驱动器 `S:`，卷名为“FQ服务器”；挂载固定使用 `--read-only`、`--network-mode` 和 `known_hosts` 校验，不保存密码或私钥内容。当前用户登录启动项 `FQServerSDrive` 已建立，用于登录后自动恢复盘符。
- 验收：真实桌面 Session 1 中的 rclone 进程和 `S:` 均已建立，Windows 报告约 98.1 GiB 总容量、6.4 GiB 已用、91.6 GiB 可用；服务器侧文件系统显示 99G、已用 6.5G、可用 88G、使用率 7%。`/opt/fengqigame` 为 `root:root` 且 `ecs-user` 无写权限，客户端与服务器权限形成双层只读边界。当前公网 IP 变化后需同步更新安全组与 UFW 的 `/32` 白名单。

## 2026-08-08 [Codex] 云助手免 SSH 发布通道、生产发布与技能固化

- 访问通道：本机安装阿里云 CLI 3.4.11 与 ECS 插件 0.7.6，使用 OAuth 配置 `fq-production` 和 RAM 用户 `fq-codex-deployer`；最小权限策略只绑定广州正式实例 `i-7xvdufe80gxzqw3oowvo` 的云助手命令、结果查询和文件投递，不持有长期 AccessKey。新增通用调用脚本，已通过真实 Agent 状态和只读远程命令验证。
- 正式发布：PR #27 已合并为 `main@bc344155`。发布载荷通过 GitHub 非正式发布传输并完成 SHA-256 校验；发布前数据库与上传卷备份 `fengqi-20260808-021720.dump`、`fengqi-uploads-20260808-021720.tar.gz` 均完成私有 OSS CRC64 校验。仅替换 app，未改变 PostgreSQL、上传卷、Caddy 或 `.env`。
- 回滚与验收：新镜像为 `sha256:5d497141e5df59a8a2e0b5081a6079e29fe26be4076a3961255e303b1584de2f`，旧镜像标签 `pre-bc344155-20260807-181921` 与源码备份 `source-pre-bc344155-20260807-181921.tar.gz` 已保留。迁移 `002_leaderboard_daily_collections.sql`、排行榜每日采集表、app/db healthy、备份 timer、新前端资源、公网 HTTP 308/HTTPS 200/WWW 301、安全响应头和缺 Key 401 均已验证。
- 技能固化：新增仓库级 `$publish-fq-admin`，覆盖“发布 FQ 后台”及同义生产部署请求；强制执行 `main`/required checks、双备份与 OSS 校验、载荷 SHA-256、app-only 更新、失败回滚、生产验收和五条会话归档，并明确排除凭据读取、地图包、数据库恢复、数据删除和基础设施变更。官方校验器在 UTF-8 模式下验证通过。
