# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-07-15 [Codex] 公开仓库并启用 main 强制检查

- 仓库：确认 `jiutubaba/FengQiGame` 已由私有改为公有；公开前只读扫描未发现 Git 历史包含阿里云 AccessKey ID、私钥正文、数据库备份或密钥文件。
- CI：提交 `a57c010` 的“文档、单元测试、构建与依赖审计”和“隔离 PostgreSQL 集成测试”均成功。
- 门禁：`main` 要求两项 CI 成功且分支保持最新，管理员同样受约束；强推和删除均被禁止，未额外强制 Pull Request 审批。

## 2026-07-15 [Codex] 完成正式生产基础设施与备份闭环

- 正式环境：购买并部署广州 ECS `8.148.249.198`，Ubuntu 24.04 上的 PostgreSQL、应用和 Caddy 正常运行；`fengqigame.com` 完成 A 记录、HTTP 308、Let's Encrypt HTTPS 和公网健康检查。
- 防火墙：阿里云安全组与 UFW 仅公开 80/443，443 UDP 支持 HTTP/3；SSH 22 仅允许管理员公网地址 `/32`，数据库和应用端口未发布公网。
- 备份：私有武汉 LRS OSS 的两类对象完成 CRC64 与 `AES256` 验证，14 天对象过期和 1 天分片清理生效；RAM 身份的删除、ACL 修改、跨 Bucket 和匿名访问均被拒绝。
- 调度与告警：systemd timer 已启用，每天北京时间 03:30 持久补跑；QQ SMTP 人工测试邮件由负责人实际收到。
- 恢复：从 OSS 下载最新数据库与上传卷副本，在独立 Compose 项目、网络和卷中恢复并通过数据库/应用健康检查；隔离容器、网络、卷、临时镜像标签和下载文件已销毁。
- 安全：曾出现在聊天中的 AccessKey 已撤销并轮换；当前 OSS 与 SMTP 凭据只保存在正式服务器 root 可读的 `0600` 文件中。

## 2026-07-15 [Codex] 建立正式 OSS 备份自动化基线

- 浏览器：确认 Windows 默认浏览器和实际扩展目标均为 Edge；GPT App 的 `chrome` 名称只是 Chromium 扩展后端，不修改本机浏览器设置。
- 实现：两类备份脚本增加可选产物返回；新增私有 OSS 上传、AES-256 请求、CRC64 校验、失败邮件告警、最小权限 RAM Policy 和 Linux systemd service/timer。
- 本机验证：数据库自定义格式备份与上传卷压缩快照均通过 `-PassThru` 返回非空文件，备份过程中应用恢复 healthy；PowerShell 与 RAM Policy JSON 语法通过。
- 远程交付：仅暂存本任务文件提交 `760b017` 并推送 `main`；GitHub CI 的质量/审计作业与 PostgreSQL 17 隔离集成作业均成功，未带入账号密码相关工作区修改。
- 外部进度：已创建独立私有 OSS Bucket，启用阻止公共访问、14 天对象过期和 1 天分片清理，并创建、绑定 RAM 最小权限用户与策略；默认 AES-256 仍需 API 查询确认，AccessKey 尚未创建。
- 阻塞与纠正：用户确认尚未购买或指定本项目正式服务器；本机旧 `fx-api-prod` SSH 配置来自其他历史项目，不得作为风起游戏基础设施。正式 timer、邮件告警和远端恢复等待可信服务器。
- 安全：正式凭据不得写入仓库或项目文档，最终有效凭据由部署者在服务器隐藏输入并以 `0600` 保存。

## 2026-07-15 [Codex] 建立远程 CI 并核对正式基础设施

- 远程 Git：确认私有 GitHub `origin` 可读写，`main` 与远程同步；新增 `.github/workflows/ci.yml` 并单独提交、推送 `daa9063`，未暂存或覆盖工作区原有修改。
- CI：Node 24 质量作业执行 AI 文档检查、8 项单元测试、生产构建与生产依赖审计；PostgreSQL 17 隔离作业执行 13 项集成测试，GitHub Actions 两个作业均成功。
- 域名核验：正式域名确定为 `fengqigame.com`，但仍在注册局审核；保留 8 条企业邮箱记录，尚未添加网站 A/CNAME，避免提前指向证书不匹配的占位页。
- 资源核验：阿里云当前没有轻量应用服务器；现有 OSS Bucket `war3mapload` 属于其他业务，未混入生产备份。
- 安全阻塞：用户确认尚未购买或指定本项目正式服务器；本机旧 SSH 别名属于其他历史项目，不能用于正式部署。正式防火墙、Compose、备份调度和独立异机副本仍未完成；告警邮箱已确认且不写入 Git 文档。
- 平台限制：当前 GitHub 套餐不支持该私有仓库的 branch protection/rulesets，CI 已运行但 required checks 尚不能强制。
