# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-09-04 [Codex] 多平台项目与反馈问卷

- 项目分组：地图后台对用户统一称为项目，新增 `kk`（KK平台）与 `oasis_qiyuan`（绿洲启元）平台元数据；既有项目默认归入 KK平台，迁移创建绿洲启元项目《地球前线》。平台只用于分组展示，`map_id` 仍是唯一租户与运行空间边界。
- 问卷链路：每个项目生成唯一公开反馈链接，玩家填写“上手难易、游戏画面、游戏玩法、等级奖励、存档养成”五项 1～5 分评分，QQ/微信至少一项以及两项选填建议；公开提交只接受活跃项目并独立限流。后台新增 `feedback.view` 地图权限，展示综合平均分、各维度平均分、联系方式与逐份反馈，数据按 `map_id` 隔离并随项目永久删除级联清理。
- 双端界面：项目中心按平台形成清晰分区；公开问卷采用深色游戏体验界面，修正评分行语义排版、压缩左侧介绍区并在桌面并排选填文本框，1280×720 页面高度由 1669px 降至 1105px。后台反馈沿用暖白工作区并与 1580px 页面主轴对齐，以链接、评分总览、筛选和反馈列表形成明确层级；评分总览压缩至约 87px 高，五项维度名称与均分同行、进度条单列下一行，综合分与全部均值同屏，联系方式改为轻量复制操作。单份反馈的五项评分作为记录摘要紧跟综合分与提交时间显示在标题栏，正文只保留优化建议和后续期待；评分标签在窄屏标题栏内自动换行。两项正文在桌面以细竖线和对称内边距分栏，窄屏改为细横线上下分段；字段标题使用辅助灰，玩家正文保持深色主阅读层。编号、时间、联系方式状态和筛选标签继续使用浅色弱层级，轻暖灰记录头与白色正文区分信息职责。评分维度调整后清除本地预览库 4 份旧口径反馈，按新字段重新生成 4 份演示反馈并汇总为 4.40 分。2560×1215、1366×768 桌面与 390×844 窄屏均完成真实浏览器复检；窄屏无横向溢出，主要操作触控高度不低于 44px，控制台无错误。
- 反馈运营：列表新增关键词、关注状态、联系方式和评分区间服务端筛选，以及时间、综合分和关注优先排序；新增共享 `☆/★` 关注、全选本页、批量关注/取消关注和二次确认永久删除。修改操作使用独立 `feedback.manage` 权限、当前地图全量归属校验、单事务与脱敏审计；QQ/微信提供“复制去添加”，不接入非官方个人账号协议。批量条按需出现，390px 下筛选为两列、按钮触控高度 44px。
- 验证：洁净工作树的 `npm run check` 通过（18 个必需入口、23 份文档、2 份生成事实、5 个单元测试文件 30/30 及生产构建）；隔离 PostgreSQL 全链路 18/18 通过，覆盖新评分列结构、旧 `cover` 与字符串评分请求拒绝、公开提交最小响应、字段约束、服务端筛选排序、查看/管理权限分离、批量关注与删除、脱敏审计和跨项目隔离；Vitest 明确排除 `.runtime`，历史发布工作树不再被重复计数；`git diff --check` 通过。生产传递依赖 `qs` 从 6.15.3 升至 6.16.0，构建期 `nanoid` 从 3.3.16 升至 3.3.18，完整与生产依赖审计均为 0。
- 合并与发布：功能 PR #50 与依赖审计补丁 PR #51 已合并，最终 `main@515b951` 的 CI `33776546185` 两项 required checks 均成功。`deploy-515b951` 保存源码包、151 文件洁净构建包和运行时 Dockerfile；发布前数据库与上传卷备份 `fengqi-20260904-000939.dump`、`fengqi-uploads-20260904-000939.tar.gz` 已完成私有 OSS CRC64 校验。正式镜像为 `sha256:9b4c13d756bd8e5f344cea5eca4d1fa8e8db03671886ec01240b3bb3d49d8ab9`，旧镜像标签 `pre-699b288-20260904-001736` 与源码备份已保留。
- 正式验收：`001`～`004` 迁移、反馈 schema、《地球前线》与平台归组、项目唯一反馈 Token、三容器 0 重启、app/db healthy、Caddy、备份 timer、外网跳转与安全头、缺 Key 401、公开问卷、151 文件静态树和入口资源哈希、近 15 分钟错误日志均通过。正式项目中心、反馈管理页和公开问卷完成 Chrome 目视检查，控制台警告/错误为 0；未向正式问卷写入演示反馈，批量关注与删除以隔离集成测试为验收边界。

## 2026-08-26 [Codex] 预加载代码编辑与发布精简

- 编辑器交互：代码区底部新增整行拖拽手柄，可在 240～960px 间上下调整，窄屏默认 420px；手柄支持键盘方向键、Home、End 和 Shift 加速，并向辅助技术报告当前高度。
- 静态检查：260ms 延迟分析与发布打包共用 `main.lua` 可达性结果；静态字符串 `require` 会检查生效链上的目标缺失、路径大小写和循环依赖。未加载文件灰显并以中划线和“未加载”标记，不参与代码波浪线、文件 Badge 或工作区问题总数，重新进入有效引用链后恢复检查；可达动态或间接 `require` 仍保守检查全部文件。点击生效文件问题可定位行列；编辑提示本身不改变协议，进入发布产物的语法错误由服务端拒绝，游戏客户端仍负责最终编译与执行门禁。
- 安全压缩：编辑器源码保持原样，单文件、多文件和旧版 `preloadCode` 接口都在管理员保存时生成独立发布产物；按 Lua Token 清理普通及长注释、缩进、行尾空格和重复空白，保留字符串、长字符串、必要间隔、Lua 5.4 局部变量属性及源码行数，纯注释归一为空。词法精简失败或产物超过 256 KiB 会在事务前拒绝发布，上一份有效产物不变；bootstrap 只读取已持久化产物。页面同时显示源码与压缩后字节数；局部变量遮蔽的同名 `require` 不作为模块引用检查。
- 可达裁剪：发布打包复用 Lua AST 引用图，从 `main.lua` 递归保留有效直接静态 `require("文件名.lua")` 可达文件；注释引用和未引用文件只保留在 `preloadWorkspace`，不进入 `preloadCode`、256 KiB 限额与 bootstrap。入口可达的动态 `require` 或间接全局 `require` 会为兼容运行时加载而保守打包全部文件，不再误裁剪旧代码；动态全量模式下所有文件都须通过语法检查。61,458 字节的三文件注释引用样本发布产物为 0 字节，减少 61,458 字节（100%）。
- 验证：全量 5 个单元测试文件共 30/30、隔离 PostgreSQL 集成测试 17/17、生产构建、生产依赖审计和 `git diff --check` 通过；补充普通/长注释、等号层级、单双引号、转义引号、长字符串、中文、纯注释归空、Token 防拼接、256 KiB 边界、重复打包、失败保留旧产物、bootstrap 兼容、请求阶段不重复精简、Token 序列、注释引用裁剪、传递可达、未加载文件跳过语法与动态引用检查，以及可达动态 `require` 全工作区兼容回归。扩展语料复检发现 luaparse 对非法 shebang 的内部异常文本，现统一为明确 Lua 语法错误且后续解析不受污染。31,015 字节合成 Lua 压缩为 16,013 字节，减少 15,002 字节（48.4%），压缩前后均为 1,002 行。
- 合并与载荷：可达裁剪 PR #46 合并为 `main@d8c7d7f`，未加载状态 PR #48 最终合并为 `main@699b288`；最终 `main` CI `32959725908` 的质量/审计与隔离 PostgreSQL 两项 required checks 均成功。`deploy-699b288` 的源码包与 145 文件洁净构建包 SHA-256 分别为 `fcf97700f91ee27871485db04aa3cd7c279846dfeb2a0638bd18d1f1b4b655c7`、`c163966d0a028cb892120cfb56e47269defe04e1e7e0624b562d9a1d2dc17c30`。
- 正式发布：发布前数据库备份 `fengqi-20260826-185846.dump` 与上传卷备份 `fengqi-uploads-20260826-185846.tar.gz` 均非空并完成私有 OSS CRC64 校验。正式 `.release-commit` 为 `699b288`，镜像为 `sha256:19cc79ee047ad406104decc66463e9a2083955965ca4c29bd4e0e167e4640754`，旧镜像标签 `pre-d8c7d7f-20260826-190000` 与旧源码包已保留；数据库、上传卷、Caddy、`.env`、网络和正式预加载数据未改变。
- 生产验收：镜像 revision、完整文件树、`001/002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、四项安全头、健康接口、缺 Key 401、近 15 分钟 error/fatal 为 0 及公网 index、主 JS/CSS、ConfigPanel 哈希均通过。生产镜像内未加载文件跳过问题检查与动态 `require` 全工作区兼容烟测通过；正式浏览器标签页读取超时，登录态配置页、真实地图 MD5/编译/执行与多人同步仍待联调。
- 当前边界：未加载文件的中划线与检查降噪已部署生产；本地合成 API 页面完成可见性验收。未修改正式血液地图的预加载源码或发布产物，未执行真实地图联调。

## 2026-08-25 [Codex] 预加载代码根目录工作区

- 界面与交互：预加载代码改为 GitHub 式根目录文件列表，固定 `main.lua` 入口，支持多个 `.lua` 文件拖入或选择导入，以及新建、重命名、删除和带行号的代码编辑；不再要求创建或维护多层文件夹，入口文件不可移动或删除，重复文件、非法扩展名、未保存修改、删除确认、只读权限、打包容量与窄屏布局均有明确状态。服务端继续读取已有虚拟目录数据，避免旧工作区不可访问，新文件统一进入根目录。
- Lua 编辑器：纯文本输入区升级为 CodeMirror，使用 VS Code Dark+ 风格的关键字、变量、函数、字符串、数字与注释配色，支持当前行、括号匹配、搜索、Tab 缩进和只读态。输入后以 Lua 5.3 语法为基线检查，并兼容中文标识符及 Lua 5.4 的 `<const>`/`<close>` 局部变量属性；错误显示红色波浪线、行号侧标记、悬浮说明和行列位置，点击状态可定位首个错误，不改变保存协议或游戏端编译门禁。
- 服务端合约：源码工作区继续保存在当前地图 `map_configs.config`，不写入上传卷；旧单段代码读取时映射为 `main.lua`，仅入口文件时保持原文本，多文件时生成带缓存和循环依赖保护的模块加载器，其他文件通过 `require("相对路径")` 使用。服务端拒绝非法层级、路径穿越、重复/冲突路径、缺失父目录和非 `.lua` 文件，最终 `preloadCode` 仍限制 256 KiB 并由 `/api/fq/bootstrap` 原样下发。
- 权限与并发：普通维护者即使具备 `map.edit` 也只能查看预加载源码；服务端仍以管理员角色作为修改边界。管理端保存携带配置更新时间，旧版本返回 409；审计只记录受影响的文件路径和打包字节数，不记录源码正文。
- 验证：`npm run check` 通过（18 个必需入口、23 份文档、2 份生成事实、16 项单元测试和生产构建），`npm run audit:prod` 为 0，`git diff --check` 通过。本地构建使用不连接数据库的合成 API 完成桌面、390px 窄屏、根目录文件选择导入、同名拒绝、编辑保存、新建、重命名、删除及普通维护者只读验收；升级后继续验证 Lua 多色高亮、正确代码通过、错误代码提示与点击行列定位，恢复原始代码后页面回到“代码已保存”。桌面预览标签页已保留；文件拖放入口已静态检查，未使用真实系统拖放复验。
- 合并与载荷：功能 PR #39 与运行镜像补充 `shared/` 的 PR #40 已合并为 `main@54e0fb1`；PR 与最终 `main` 的质量/审计、隔离 PostgreSQL 两项 required checks 均通过，最终 CI 为 `32863646149`。`deploy-54e0fb1` 的源码包与 145 文件洁净构建包 SHA-256 分别为 `630c93d2349efb746a2666ace6ede3b9459995a69eb2ccfc141a4719db3f6cd6`、`6e000933e8820b5cef3edd306b0ba7c84a92e75080d06c9264eadbd7efa39fe3`。
- 正式发布：数据库与上传卷备份 `20260825-230326/230327` 均完成私有 OSS CRC64 校验；app-only 发布后的正式镜像为 `sha256:1b9cf801bcf09ebc6f4b3a2c05b52cc1e52caf26fc34a02d3ed11b314bb237ac`，`.release-commit` 为 `54e0fb1`。旧正式镜像、校正前健康镜像和旧源码包均保留；未改变 PostgreSQL、上传卷、Caddy、`.env` 或网络。
- 生产验收：`001`、`002`、排行榜每日采集表、`players.rank_ban`、共享预加载模块、三容器 0 重启、app/db healthy、Caddy、备份 timer、公网跳转、安全头、健康接口、缺 Key 401、近 15 分钟日志、145 文件整树及公网 index、主 JS/CSS、ConfigPanel 哈希均通过。地图 ID 2 当前为 56,733 字节旧格式 `preloadCode`，尚无结构化 `preloadWorkspace`；新页面读取时映射为 `main.lua`。内置浏览器能打开正式登录页并读取标题“风起游戏”，但 DOM/Canvas 与控制台读取超时；正式登录后的配置页交互、真实系统拖放和游戏客户端仍待验收。

## 2026-08-24 [Codex] FQ 预加载代码正式发布

- 发布内容：`/api/fq/bootstrap` 下发最大 256 KiB 的 `preloadCode`，管理员配置页显示 UTF-8 容量 `当前KB/256KB`；普通地图维护者只读。投递请求在 `includeMessages=false` 时只需 `game.gifts.read`，同时删除客户端不消费的响应字段。
- 代码与载荷：PR #37 合并为 `main@18f612c`，PR 与 `main` 的质量/审计、隔离 PostgreSQL 两项 required checks 均通过，`main` CI 为 `32740774916`。`deploy-18f612c` 的源码包与 145 文件洁净构建包 SHA-256 分别为 `7059fafc0e45e118eefea1a2be235fcf8f3534f679293f717c342a8dd561a8da`、`9f22801b2ed57032e628cbc65e6e28dba9a78c36d152f965c5fd1f5e03ed820d`。
- 正式发布：数据库与上传卷备份 `20260824-224921/224922` 均完成私有 OSS CRC64 校验；云助手 app-only 发布后，正式 `.release-commit` 为 `18f612c`，镜像为 `sha256:da23617d9be40b00388e1e51afc5de38c180e7e4993def28000d5eef1cf44b3a`，旧镜像与旧源码包已保留。app/db、Caddy、备份 timer、迁移、公网跳转、安全头、健康接口、缺 Key 401、日志及静态资源哈希均通过，未改变数据库、上传卷、Caddy、`.env` 或网络。
- 剩余边界：地图 ID 2 的预加载代码仍为空，正式登录后的配置页视觉状态未验收；游戏地图未重新构建、发布或实机联调。旧地图省略 `includeMessages` 时服务端兼容默认 `true`，因此血液地图 Key 暂时保留 `game.messages.read`，必须先发布固定发送 `false` 的新地图并验证正式请求，再回收权限。

## 2026-08-17 [Codex] 登录页 Galaxy 动态视觉升级

- 视觉与边界：登录页左侧由浅色网格升级为深石墨动态星海，保留品牌主标题、地图隔离说明、返回首页与安全会话状态；右侧暖白登录表单、账号密码交互、错误反馈、跳转和认证接口均未改变。视觉规范明确该深色画布只属于登录品牌入口，不扩散到登录后业务工作区。
- 组件与性能：新增基于 React Bits Galaxy 思路和 `ogl` 的 WebGL 组件，使用稳定的标量依赖避免表单重渲染反复创建上下文；通过 `ResizeObserver`、`IntersectionObserver`、页面可见性、`prefers-reduced-motion` 与 680px 移动断点控制渲染，隐藏的窄屏画布缩至 1×1，鼠标交互由父级承接且不拦截链接或表单。WebGL 不可用时保留静态星点背景。
- 验证：`npm run check` 通过（18 个必需入口、23 份文档、2 份生成事实、13 项单元测试与生产构建），`npm run audit:prod` 为 0，`git diff --check` 通过。本地浏览器完成 1440×900 桌面与 390×844 窄屏验收：桌面 Galaxy 状态为 ready 且仅有一个 755×900 画布；窄屏左侧隐藏、画布为 1×1、页面无横向溢出，控制台无警告或错误；媒体仿真确认减少动态偏好生效。预览使用 Vite 静态构建，真实 API 未启动，未提交真实账号或执行登录。
- 合并与载荷：PR #34 合并为 `main@82f614b`，PR 与 `main` CI 运行 `31979948451` 的质量/审计和隔离 PostgreSQL 两项 required checks 均通过。GitHub 非正式发布 `deploy-82f614b` 的源码包 SHA-256 为 `1e1da887430ccd08dcdf99ac3bef93bc67d437b354e61c7034cca119e419cc2d`；发现本机既有 `dist` 混有历史未引用哈希文件后，没有沿用该目录，而是改在全新隔离目录构建 145 个当前文件，正式构建包 SHA-256 为 `741917453dc0500df9b387712b740678354c9ce745a61c582f74b6a14d6df1a3`。
- 正式发布：发布前数据库备份 `fengqi-20260817-074840.dump` 与上传卷备份 `fengqi-uploads-20260817-074841.tar.gz` 均非空并完成私有 OSS CRC64 校验。云助手 app-only 发布后，正式 `.release-commit` 为 `82f614b`，镜像为 `sha256:77862f1739b56316190463a0f672ffeab3e9cfca3d45800a97e02ff65e88ce24`；旧镜像标签 `pre-10f453c-20260817-075112`、校正前健康镜像标签 `pre-dist-cleanup-82f614b-20260817-075356` 与源码备份 `source-pre-10f453c-20260817-075112.tar.gz` 已保留，未改变 PostgreSQL、上传卷、Caddy、`.env` 或网络。
- 生产验收：`001`、`002` 迁移登记与关键表/字段存在，app/db healthy、Caddy running、备份 timer active/enabled，容器内与公网健康接口 200；公网 HTTP 308、`www` 保留 URI 301、四项安全头、缺 Key 401、新前端四个入口资源哈希和近 15 分钟 app error/fatal 为 0 均通过。内置浏览器能导航至 `https://fengqigame.com/login` 并读取标题“风起游戏”，但 DOM/Canvas 与控制台读取连续超时；正式 WebGL 可见状态和控制台未完成，未将 HTTP、哈希或本地视觉验收冒充为正式浏览器交互验收。
