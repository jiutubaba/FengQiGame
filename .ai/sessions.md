# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-09-09 [Codex] 排行榜批量移除

- 实时榜增加复选框、全选当前显示和批量移除确认，一批最多 100 条。切换榜单、地图、搜索或快照清空选择；失败保留选择并持续显示错误，处理期间禁用重复操作，复选框点击区域至少 44px。
- 新增 POST /api/maps/:mapId/leaderboards/:leaderboardId/entries/batch-delete，要求 leaderboards.manage、唯一条目 ID 数组与 confirm=true。事务内锁定并核验全部条目归属，删除与审计共同提交，缺失或跨榜跨地图整批拒绝；历史快照和每日采集事实不变。
- npm run check 通过（30 项单元测试及构建），独立临时 PostgreSQL fengqi_test 库内 21 项集成测试通过，生产依赖审计 0 漏洞。新增验证覆盖确认字段、数量和重复校验、权限、归属、整批回滚、审计、快照及当日重报；临时测试库已清理。
- 浏览器在 localhost:4174 合成数据预览验证单选/全选、取消、删除后空状态、失败重试、搜索清空选择、快照只读和查看权限隐藏；390px 窄屏工具栏与选择列可达，控制台无警告/错误。已恢复桌面并保留预览标签；没有删除业务排行榜记录。
- 已更新本地 app，未正式发布或提交 Git；此前排行榜上限、multer 和启动修复改动保留。

## 2026-09-09 [Codex] multer 上传安全修复

- multer 从 2.2.0 升级至 2.3.0 并更新 npm 锁文件；上传文本字段不使用数组索引，设置 fieldArrayIndexLimit=0，复用原有 MulterError 的 400 响应。
- 上传全链路补充正数组索引与超大文件拒绝验证，确认拒绝请求没有文件元数据写入；原有正常上传下载、危险扩展名拒绝与目录级联删除均通过。
- Node 24 容器内 npm run check 通过（30 项单元测试及生产构建），独立临时 PostgreSQL fengqi_test 库内 20 项集成测试通过（UPLOAD_MAX_MB=1），npm run audit:prod 为 0 漏洞。
- Docker 再次遇到遗留 socket 启动故障，备份运行时目录后恢复。本地更新曾返回 Compose 临时容器名冲突，复核时旧容器已消失；运行镜像与 latest 一致，multer 实际版本 2.3.0，健康接口 status=ok。未执行正式发布或 Git 提交；此前排行榜上限和启动修复改动保留。

## 2026-09-09 [Codex] 排行榜数值范围扩大

- 用户选择保持数值协议；score 范围由 ±1e15 扩大到 Number.MIN_SAFE_INTEGER～Number.MAX_SAFE_INTEGER（±9,007,199,254,740,991）。保持已有小数支持；整数精确性有保证，小数仍受 Number 精度限制，无数据库迁移。
- 同步客户端接口说明与排行榜、客户端系统文档；新增大整数端到端边界测试，覆盖最大值、相邻整数、超过旧上限、小数、最小值、字符串输入、精确入库、排序、发布回读和越界整批拒绝。
- Node 24 容器内 npm run check 通过（30 项单元测试及构建）；独立临时 PostgreSQL fengqi_test 库内 npm run test:integration 20/20 通过，临时库容器已移除。npm run audit:prod 仍为既有 multer 1 项 high，见 backlog。
- 本次仅更新本地服务；未提交 Git、未发布正式环境，保留此前启动修复及用户修改。

## 2026-09-09 [Codex] 本机首次启动修复

- Docker Desktop 因遗留零字节 socket 重解析点无法访问而启动失败；停止故障进程，将仅含 socket 的两个运行时目录改名备份后重启成功，未使用恢复出厂设置。
- 首次配置脚本增加 UTF-8 BOM，Windows PowerShell 5.1 语法解析通过；Dockerfile 构建阶段补充 shared 目录，修复前端预加载模块无法解析。
- 用户完成本机首次配置；Compose 数据库、应用与 Caddy 启动成功，http://localhost 首页 HTTP 200、健康接口 status=ok。管理员初始化密码已从 .env 移除并重建应用容器，应用再次健康。
- Node 24 一次性容器内 npm run check 通过：文档检查、30 项单元测试与生产构建；npm run audit:prod 报 multer 1 项 high，已写入 backlog。本次未运行数据库集成测试、HTTPS 或备份恢复演练。
- 保留任务开始前启动网站.bat 的未提交修改；未进行 Git 提交或正式部署。

## 2026-09-07 [Codex] 排行榜实时采集策略

- 新增“实时更新样本”和“实时更新更优样本”，每日首份两种策略保持不变；分数与附属字段按同一规则更新，更优策略相同成绩保留达成时间。
- 实时策略返回空的 `submittedTodayUids`，允许客户端继续上报；每日采集事实仍保留，切回每日策略后不能同日再次采集。游戏查询继续读取人工发布快照。
- 迁移 `005_leaderboard_realtime_modes.sql` 只扩展策略约束，不修改已有榜单配置或成绩；客户端接入、接口技术说明与系统合约已同步。
- 本地 `npm run check` 通过（30 项单元测试与生产构建），生产依赖审计为 0；CI 隔离 PostgreSQL 全链路 19/19 通过，包含四策略、升降序、同日重报、并发更优、相同分数、策略切换和快照隔离。本地 Chrome 合成数据预览已完成新增选项选择、保存、回显和桌面布局验收，预览标签页保留；本机隔离库不可用，数据库验证在 CI 隔离库完成。
- 发布：PR #54 合并为 `main@a1c424c`，`main` CI `34091731897` 两项 required checks 均通过。2026-09-07 完成双备份与 OSS CRC64 校验，通过云助手只更新 app，迁移 `005` 与四策略约束已生效；版本、容器健康、公网鉴权/跳转、安全头、静态资源哈希、备份 timer 和日志验收通过，回滚镜像与旧源码已保留，详见 [部署与运维](../docs/部署与运维.md)。
- 剩余验收：正式浏览器控制连续出现连接超时与 `Debugger unattached`，未完成登录后页面目视和真实游戏上报联调；已有榜单策略与历史快照未自动修改。
