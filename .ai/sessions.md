# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-07-14 [Codex] 完成本机 Docker 与恢复链路验收

- 目标：在开启 SVM 并重启后继续生产部署验收，补齐数据库与上传文件恢复闭环。
- 环境：安装当前用户级 Docker Desktop 4.82.0，确认 Linux Engine 29.6.1、16 CPU 与约 16 GB 引擎内存可用。
- 修复：在 Linux/Node 24/npm 11.16 环境同步 `package-lock.json`，补齐跨平台可选依赖，生产镜像的两次 `npm ci` 均通过；补充响应 `Set-Cookie` 日志脱敏，避免会话 Token 进入应用日志。
- 容器验收：隔离 Compose 项目的 PostgreSQL、应用和 Caddy 启动成功；HTTP 健康接口、响应头、管理员与普通用户服务端授权通过，一次性管理员密码从 `.env` 和容器环境移除。
- 恢复验收：数据库自定义格式备份非空，隔离变更在恢复后消失；上传卷从 `mutated` 恢复到 `baseline`，额外文件被清除。两类恢复都自动生成安全副本并等待应用健康。
- 浏览器验收：首页、登录页、管理员登录后的地图中心正常渲染，浏览器控制台 0 个警告/错误。
- 清理：验收容器、网络、4 个命名卷、本地应用镜像、临时 `.env`、数据库备份、上传快照和测试文件全部销毁。
- 未完成外部项：真实域名 HTTPS、正式服务器防火墙、备份调度、异机副本、告警负责人、远程 Git/CI 继续保留在 backlog。

## 2026-07-14 [Codex] 建立 AI 长期开发治理基线

- 目标：为风起游戏建立生产级 `.ai` 知识库、事实生成与一致性检查、本地 Git 基线。
- 事实来源：当前 React/Express/PostgreSQL 源码、迁移、权限中间件、测试、部署脚本和现有 `docs/`。
- 范围：只增加开发治理、工具和版本控制，不改变业务 API、数据库行为或界面功能。
- 验证：治理检查通过（18 个必需入口、21 份文档、2 份生成事实）；上下文路由覆盖客户端、地图工作台和恢复脚本；连续生成结果一致；临时副本修改路由后检查按预期失败。
- 质量结果：单元测试 7/7、PostgreSQL 集成测试 13/13、生产构建和 PowerShell 语法通过，生产依赖审计为 0 个漏洞；测试数据库和漂移测试副本均已清理。
- 版本控制：本归档随 `main` 分支首个治理基线提交进入 Git，不配置远程仓库。
- 未完成外部项：正式 Docker/HTTPS、真实域名和备份恢复演练继续保留在 backlog。
