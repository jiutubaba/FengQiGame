# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-07-20 [Codex] 备案通过并部署正式上线候选

- 合规：确认 ICP 网站备案 `鄂ICP备2024058688号-2` 已通过，公共首页页脚新增备案号并链接工信部查询页；公安联网备案仍须在网站开通后 30 日内提交，审核通过后再追加公安备案号。
- 发布内容：保留并纳入现有视觉重构、外来工具/文档移除和管理员地图永久删除全部改动；永久删除使用双重确认、事务与上传目录回滚，保留历史审计和既有备份。
- 验证：AI 文档检查、12 项单元测试、生产构建、生产依赖审计和本机隔离 PostgreSQL 15 项集成测试通过；发布前数据库及上传卷副本均上传私有 OSS 并完成远端校验。
- 正式部署：应用源码提交 `b8f17d9` 的候选镜像 `sha256:ce93d8eb495fc9e01eeaea33ec70a9c88ad99c7689a0a85a0f876f868aa8a3ba` 已部署到广州 ECS，app healthy；`.env`、PostgreSQL、Caddy 和正式空业务库未改变，服务器本机 HTTPS、HTTP 308、FQ 鉴权与永久删除鉴权复验通过。
- 公网验收：阿里云权威 DNS、223.5.5.5 与 1.1.1.1 均返回正式 ECS；公网 HTTP 308、HTTPS 健康接口 200、Let's Encrypt 证书、HSTS/CSP、备案号和外来工具入口移除均复验通过。
- 远程交付：PR #8 已合并为 `3d2df971`，`main` CI 运行 `29732388086` 成功，两项 required checks 均通过。
- FQ 正式冒烟：正式库地图“沧澜”默认环境为 `release`，`release`、`lobby`、`test` 三套最小权限 Key 完成鉴权、玩家写入、玩家存档首次保存、幂等重放、requestId 复用拦截、revision 冲突、权限边界与同 UID 隔离验证；结束后删除 3 条存档和 3 条玩家测试记录，残留为 0，服务器临时凭据与脚本已删除。
- 待收尾：完成公安联网备案；由《沧澜》游戏项目接入 FQ 协议，并在 `test` 环境验收实际客户端的全局存档、封禁、消息和礼包链路。

## 2026-07-16 [Codex] 新增管理员永久删除地图

- 交互：地图配置新增独立永久删除危险区，使用连续两次危险弹窗说明三环境数据、API Key、上传文件和不可恢复后果；归档入口及语义保持不变。
- 服务端：新增管理员 `DELETE /api/maps/:mapId/permanent`，再次核对地图 ID 与名称；上传目录先原子暂存，数据库事务内锁定地图、写入 `map.delete` 审计并删除关联数据，失败恢复目录，成功清理暂存目录和原目录。
- 残留与边界：启动时只重试地图已不存在的受控 `.deleting-map-*` 目录；永久删除保留历史审计和既有数据库/上传卷/OSS 备份，完整 API Key 随数据库记录失效。
- 验证：AI 文档检查、12 项单元测试、生产构建、生产依赖审计和隔离 PostgreSQL 15 项集成测试通过；集成测试覆盖权限、确认不匹配、强制事务失败回滚、三环境及子表级联、重复请求、审计保留、API Key 失效和磁盘无残留。

## 2026-07-16 [Codex] 发布 FQ 服务器接口与存档链路

- 协议：服务器专属游戏接口由 `/api/game`、`X-Map-Key` 全量切换为 `/api/fq`、`FQ-Map-Key`，错误码同步使用 `FQ_` 前缀，不保留 X/XY 兼容入口。
- 存档：新增玩家与全局 JSON 快照，按 `map_id`、`environment` 隔离；保存要求 `FQ-` requestId、完整快照和 revision，支持最近一次请求幂等、乐观并发控制、并发首次写入、封禁阻断、UID 迁移和地图运行数据清理。
- 客户端边界：已补充 `/api/fq/bootstrap`、玩家/全局存档读写协议，并新增可直接移交游戏项目的 `docs/FQ游戏服务器接口技术说明.md`，覆盖 15 个接口、字段约束、错误码、重试策略与《沧澜》Yasio 调用示例；地图 Lua 代码未修改，仍由游戏开发方负责接入、字段映射和 Base64 兼容决策。
- 验证：8 项单元测试、隔离 PostgreSQL 14 项集成测试、生产构建、生产依赖审计、AI 文档检查和 GitHub CI 运行 `29487045379` 均通过。
- 发布：PR #5 合并为 `8bb9573`；同一版本镜像已部署到广州 ECS，app healthy 且镜像 ID 一致，`004_fq_archives.sql`、两张存档表、FQ 缺密钥 401 和旧路由 404 均复验通过，PostgreSQL 与 Caddy 未重建。

## 2026-07-15 [Codex] 移除外来工具版本与 XYPlugin 文档

- 范围：确认“工具版本”和“XYPlugin 文档”没有对应服务端 API、数据库表或风起游戏业务链，仅由前端页面、路由、导航入口、首页宣传和随站点发布的静态档案组成。
- 实现：删除 `/tools`、`/docs` 路由及页面，移除侧栏、顶栏和公共首页中的全部入口与宣传内容，并清理仅供这两类页面使用的样式。
- 资源：删除 `public/data/` 下 XYWE 版本页、模型版本页以及 XYPlugin 首页、文档、Lua 模块和更新记录共 6 份静态档案；README 不再声明或发布这些外来资料。
- 边界：地图中心、账号权限、玩家数据、正式数据库、备份和告警链路均未改动。
- 验证：仓库残留检索、AI 文档检查、8 项单元测试、生产构建、生产依赖审计和隔离 PostgreSQL 13 项集成测试通过；浏览器验证登录前首页、登录后后台和旧路由回退均无外来文案或链接，控制台无警告或错误。

## 2026-07-15 [Codex] 完成 Attio 风格全站视觉与后台密度精修

- 视觉：将原深色能量风格改为温暖白与雾灰画布、黑色精密文字和单一低饱和蓝色强调；统一首页、登录、抽奖、后台壳层、地图工作台、工具、文档、个人中心与管理员页面的字体、密度、控件状态、表格、模态框、Toast、空态和移动布局，业务逻辑、API 与权限链保持不变。
- 品牌与层级：保留 FQ 标记结构并改为黑、白、淡蓝配色；公共页面使用低对比网格和大留白，后台使用侧栏、1px 边界、表格与列表建立信息层级，移除渐变光晕、重阴影和无意义卡片感。
- 字体与可访问性：通过 Fontsource 本地托管 `Noto Sans SC Variable` 与 `Noto Serif SC Variable`；业务辅助文字不低于 11px，窄屏主要点击区域不低于 44px，保留键盘焦点、图标按钮名称和 `prefers-reduced-motion` 降级。
- 字号与动效：常用导航与页面说明使用 13–14px，指标与空态辅助说明使用 12–13px，11px 仅用于编号和技术元数据；圆角收敛为 6–12px，页面、行、按钮、抽屉与模态框反馈统一为约 140–200ms。
- 后台密度：页面标题统一为 26px，工具栏约 52px，指标格约 120px，表格表头与数据行分别为 40px、48px；配置、礼包、风控、文件、文档、工具、个人中心和管理员面板收敛无效留白与重阴影。
- 地图中心：卡片网格按 4/3/2/1 列响应式排列，封面收敛到 136–158px，占位图改为浅灰网格与居中 FQ 标记；列表标题与指标横向整理，桌面列表行稳定为 120px。
- 浏览器验收：使用独立候选容器、空数据库、一次性管理员、受限用户和测试地图；在 1440×900、1024×768、390×844 下检查 17 个登录后路由、地图创建、账号创建、最小权限、未授权管理员路由和移动侧栏，均无横向溢出，移动菜单按钮为 44px，控制台无警告或错误。
- 验证：隔离 PostgreSQL 集成测试 13 项通过，明确覆盖 `release`、`lobby`、`test` 同 UID 隔离以及 5 位拒绝、6 位密码修改重登；AI 文档检查、8 项单元测试、生产构建和生产依赖审计通过，隔离容器、网络、卷和临时镜像已销毁。
- 发布：PR #2 合并为 `09ace64`，`main` CI 运行 `29401624116` 成功；同一提交的镜像已部署到广州 ECS，正式 app 与镜像 ID 一致且为 healthy，数据库与 Caddy 未重建，服务器本机 HTTPS 健康接口、HTTP 308、证书、静态资源和未认证 401 均通过。
- 恢复复验：发布前手工触发正式备份，systemd 返回成功并完成两类 OSS CRC64 校验；随后从私有 OSS 实际下载最新副本，在临时网络和卷中恢复出 26 张业务表，隔离应用达到 healthy，临时容器、网络、卷、下载文件和发布归档均已清理。
