# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-08-09 [Codex] 前端错误恢复、提交状态与危险操作体验统一

- 通用交互：新增可重试错误状态、页面内提示和统一确认弹窗；模态框补齐初始焦点、Tab 焦点循环、Escape、背景关闭控制、滚动锁定与关闭后焦点归还，表单字段支持就地错误说明。
- 页面接入：登录、地图中心、审计、账号与权限、系统设置、个人资料和地图工作台在加载或保存失败时保留原位反馈；设置、资料、账号、权限和地图配置补齐保存中状态、防重复提交与未保存提示。地图归档、玩家删除、排行榜发布或删除、风控规则、礼包、群抽、主播点位、文件和 API Key 等危险操作统一为站内确认体验，未改变既有权限、接口参数或服务端校验。
- 导航与数据操作：登录后顶栏补齐地图和当前功能面包屑，“地图中心”和当前地图均可点击直达，当前页保留不可点击的位置语义；390px 窄屏新增地图功能横向快速切换。玩家、风控、排行榜与账号列表统一展示当前筛选摘要，玩家、风控和账号关键筛选写入 URL 并支持刷新恢复。窄屏数据表增加横向滑动提示并固定关键操作列，未改变接口、权限或业务状态语义。
- 排行榜快捷封禁：实时榜条目在“移除”后新增“拉黑”，排行榜管理者确认后按地图和 UID 写入玩家 `rank_ban=true` 并记录 `leaderboard.player.ban` 审计；该玩家从当前地图所有实时榜和后续发布中排除，候选条目、每日采集事实与已发布历史快照保持不变，可在玩家管理取消排行榜封禁。排行榜操作列在桌面和窄屏均固定于右侧。
- 验证：`npm run check` 通过（18 个必需入口、23 份文档、13 项单元测试与生产构建），`npm run audit:prod` 为 0；Vite 与 React 插件按实际用途归入构建期开发依赖，不进入正式运行镜像。临时隔离 PostgreSQL 全链路 17/17 通过，覆盖快捷封禁的管理权限、玩家状态、实时榜排除、历史快照不可变与审计。本地浏览器完成登录页及登录后页面的桌面与 390px 窄屏验收，覆盖导航切换、面包屑两级直达、筛选 URL 持久化、排行榜“移除 / 拉黑”操作列和确认弹窗，控制台无页面运行异常；未连接正式或共享数据。

## 2026-08-08 [Codex] 正式服务器只读 S 盘映射

- 访问边界：只为本机当前真实公网 IPv4 在正式 ECS 安全组和 UFW 增加 TCP 22 的单地址 `/32` 规则；网页代理出口的误判规则已在验证后从两层防火墙撤销。`sshd`、SFTP 子系统、主机指纹和现有 `ecs-user` 身份文件均已通过真实连接验证，没有开放网段、密码登录、root 直登或数据库端口。
- 本机挂载：安装 WinFsp 2.1.25156 与 rclone 1.75.0，通过 SFTP 将正式 `/opt/fengqigame` 映射为网络驱动器 `S:`，卷名为“FQ服务器”；挂载固定使用 `--read-only`、`--network-mode` 和 `known_hosts` 校验，不保存密码或私钥内容。当前用户登录启动项 `FQServerSDrive` 已建立，用于登录后自动恢复盘符。
- 验收：真实桌面 Session 1 中的 rclone 进程和 `S:` 均已建立，Windows 报告约 98.1 GiB 总容量、6.4 GiB 已用、91.6 GiB 可用；服务器侧文件系统显示 99G、已用 6.5G、可用 88G、使用率 7%。`/opt/fengqigame` 为 `root:root` 且 `ecs-user` 无写权限，客户端与服务器权限形成双层只读边界。当前公网 IP 变化后需同步更新安全组与 UFW 的 `/32` 白名单。

## 2026-08-08 [Codex] 云助手免 SSH 发布通道、生产发布与技能固化

- 访问通道：本机安装阿里云 CLI 3.4.11 与 ECS 插件 0.7.6，使用 OAuth 配置 `fq-production` 和 RAM 用户 `fq-codex-deployer`；最小权限策略只绑定广州正式实例 `i-7xvdufe80gxzqw3oowvo` 的云助手命令、结果查询和文件投递，不持有长期 AccessKey。新增通用调用脚本，已通过真实 Agent 状态和只读远程命令验证。
- 正式发布：PR #27 已合并为 `main@bc344155`。发布载荷通过 GitHub 非正式发布传输并完成 SHA-256 校验；发布前数据库与上传卷备份 `fengqi-20260808-021720.dump`、`fengqi-uploads-20260808-021720.tar.gz` 均完成私有 OSS CRC64 校验。仅替换 app，未改变 PostgreSQL、上传卷、Caddy 或 `.env`。
- 回滚与验收：新镜像为 `sha256:5d497141e5df59a8a2e0b5081a6079e29fe26be4076a3961255e303b1584de2f`，旧镜像标签 `pre-bc344155-20260807-181921` 与源码备份 `source-pre-bc344155-20260807-181921.tar.gz` 已保留。迁移 `002_leaderboard_daily_collections.sql`、排行榜每日采集表、app/db healthy、备份 timer、新前端资源、公网 HTTP 308/HTTPS 200/WWW 301、安全响应头和缺 Key 401 均已验证。
- 技能固化：新增仓库级 `$publish-fq-admin`，覆盖“发布 FQ 后台”及同义生产部署请求；强制执行 `main`/required checks、双备份与 OSS 校验、载荷 SHA-256、app-only 更新、失败回滚、生产验收和五条会话归档，并明确排除凭据读取、地图包、数据库恢复、数据删除和基础设施变更。官方校验器在 UTF-8 模式下验证通过。

## 2026-08-07 [Codex] 数据中心、玩家管理与排行榜正确性完善

- 数据中心：参考 KK 的口径说明交互，为 11 项指标补充可聚焦且始终向上展开的帮助说明、数据源/北京时间/统计纪元信息、分子分母与无样本状态；指标卡可切换对应近 30 日趋势，节点支持鼠标、触屏和键盘查看完整日期与精确值。服务端修正跨日重放、结束后心跳、复玩日期归属和旧快照默认日期，并返回留存/复玩的样本数；“总局数”统一改为服务端观测时长严格超过 10 分钟的游戏有效局，正好 10 分钟不计。
- 玩家管理：等级列只显示数值等级，不再把 `gameLevel` 拼成 `x/x`；等级和最后活跃改为服务端全量排序，列表使用服务端真实分页与全量搜索，并补充升降序表头、请求竞态保护、消息独立加载和不可见选择清理。已有会话、存档、礼包、风控、榜单或最近活跃记录的玩家由服务端锁定 UID，仅无业务数据的占位玩家仍可改名。
- 礼包资格：新增仅需 `gifts.manage` 的最小字段玩家分页接口，资格选择器按全量玩家真实分页与搜索，每页固定展示 10 人；礼包目录每行提供独立筛选按钮，可多选并按“拥有任一所选礼包”筛选正数资格玩家，且与名称或 UID 搜索共同生效。跨页选择保持、单次最多 500 人，批量设置后只更新已加载资格缓存，不再一次拉取整张地图的玩家及资格。礼包资格与群抽活动改为可访问页签；三栏资格工作区按视口自适应高度并独立滚动，筛选和翻页使用轻量骨架反馈；目录由服务端返回整图正数资格人数，并同时展示启用状态。
- 群抽活动：保留进行中活动的复制、开奖和取消；已开奖、已取消或已到计划开奖时间的活动可从后台永久删除。服务端在事务内按地图锁定并复核状态，删除报名和开奖结果的同时写入 `lottery.delete` 审计，有效活动返回明确冲突，前端使用不可恢复确认弹窗并将逾期状态明确标记为“已过期”。
- 排行榜：发布上限与游戏侧本人名次统一限定前 100；新增独立每日采集门闩，删除候选不再允许同 UID 当日重报；Key 创建后锁定，最新快照排序稳定，空候选禁止发布，候选/发布状态和每日首份策略文案分开显示。
- 血液等级榜诊断：FQ 已有 `map_level` 定义，当前地图源码也已有结算上报；尚无证据证明最新发布地图包已包含该代码和新 Key，也未确认线上候选池/快照，因此仍需重新构建地图并完成一次真实结算后检查候选，再人工发布前 100 名。
- 验证：`npm run check` 通过（13 项单元测试与生产构建），生产依赖漏洞为 0；隔离 PostgreSQL 全链路 17/17 通过，覆盖日期格式、有效局严格阈值与跨日归属、玩家分页/排序/UID 锁定、礼包资格每页 10 人、权限、筛选叠加与整图资格人数、群抽永久删除权限/状态/级联/审计、排行榜 Key/前 100/删除后同日重报和运行数据清理。Chrome 已完成桌面及 720px 窄屏验收，并保留带合成数据的本地预览页。本条开发完成时尚未部署；2026-08-08 后续归档已记录正式发布，地图内联调仍待执行。

## 2026-08-05 [Codex] FQ 单运行空间生产重建与双地图切换归档

- 架构与代码：FQ 已彻底删除 `release`、`lobby`、`test` 业务维度，数据库、后台 API、游戏协议和前端只按 `map_id` 隔离；新迁移历史只包含 `001_single_map_baseline.sql`，ADR-0005 替代旧环境边界。PR #27 的分支 `codex/fq-single-runtime-space@2a91da9` 两项 CI 均通过，生产已部署同一提交。
- 生产重建：正式 Compose 改用 `postgres_single_data` 与 `uploads_single_data` 全新空卷；旧 `postgres_data`、`uploads_data` 离线保留，`.env`、Caddy 配置/证书卷和既有 OSS 历史备份未改。本次按明确授权不迁移、不创建新备份，失败只向前修复新空实例。
- 最小状态与验收：管理员“风起”、地图 ID 1《沧澜》和 ID 2《血液锦标赛2》、35 个礼包、`landing_power`、`map_level`、`win_rate` 及两把最小权限新 Key 已恢复。两 Key 的空档 bootstrap、存档/榜单与血液指标生产烟测完成后按地图清理，玩家、存档、指标、榜单条目、快照、会话和审计均为 0；公网健康 200、未登录 401、HTTP 308、WWW 301。
- 交付边界：两地图源码已写入各自新 Key 并通过静态回归，但未由 AI 构建或覆盖 `.w3x`；仍需用户手工重新构建、发布和完成多人 UID、revision、消息 ACK、礼包、排行榜及血液指标实机联调。旧地图包和旧 Key 已失效。
