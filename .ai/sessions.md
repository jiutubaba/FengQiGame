# 近期开发归档

只保留最近 5 次重要开发任务。新增一条时删除最旧一条；稳定事实必须同步到系统文档。

## 2026-09-17 [Codex] 每图可选的通用玩法分析

- Issue #65，分支 `codex/map-optional-analytics`。新增关卡表现、选择偏好、挑战表现、阶段流失四个可选模块；地图自行定义对象，新模块默认关闭，不包含技能通关率。管理员在项目配置的开放功能中保存独立开关，服务端同步限制查询/上报并保留历史，保存与审计同事务且校验配置版本。
- 新增 `006_optional_analytics.sql` 与通用 start/end/choice 协议；同地图事件幂等、跨日归属开始日、人数去重与次数分开、未结算不推断退出。分析数据纳入玩家 UID 锁定、运行清理和地图级联删除。接入字段与规则同步到客户端文档和系统文档。
- 验证：31 项单元测试、25 项隔离 PostgreSQL 集成测试、生产构建和生产依赖审计通过。集成覆盖开关/权限/并发版本、重复事件、非法输入、跨日、地图隔离、候选被选率、挑战/阶段结果、清理保留开关。补正既有审计验收只看第一页的假设；未改审计业务。
- 浏览器：真实 API 连接独立本机 `fengqi_preview`，使用合成演示项目；已验证关闭入口隐藏、直达拦截、重新开启保留历史、版本筛选空状态、不同地图开关隔离、四类报表及 390px 窄屏无页面横向溢出，控制台无错误。人工验收使用 `http://127.0.0.1:3000/maps/2/analysis-progression`，配置入口为同地图 `/config` 的“开放功能”。
- 未验证：真实地图上报与正式部署；本次只交付本地预览，地图端应按需接入，未改任何地图源码。

## 2026-09-10 [Codex] 排行榜战力数值格式

- 对照两个刀塔地图工程的 `scripts/common/math.lua`，为后台实时榜和历史快照接入 `math.format` 单位门槛；按用户后续要求，换算后的数值与非整数保留两位小数，层数等未换算的整数直接展示，悬停保留原始成绩；数据存储、排序、客户端协议与其他计数展示不变。
- 文档检查、31 项单元测试和生产构建通过；临时 npm 工具补齐后 `npm run check` 与 `npm run audit:prod` 完成，审计 0 漏洞。PR #61 及 main CI 的两项 required checks 成功，隔离 PostgreSQL 集成测试 23 项通过。
- 浏览器使用明确标注的合成数据验证万/亿/兆、实时榜和历史快照、原值 title，控制台无警告或错误；预览保留在 `http://127.0.0.1:4175/maps/1/leaderboards`。
- 已合并 PR #61 为 `main@4c6ad6c`，main CI `34452514057` 通过。干净提交和独立 lockfile 安装生成发布包 `deploy-4c6ad6c`，双备份非空且完成 OSS CRC64 校验后成功 app-only 部署；版本、镜像、151 文件静态树、7 个公网资源、迁移、健康、跳转、鉴权、安全头、timer 和日志均通过，旧镜像与源码保留。正式浏览器跳转登录页且控制台无错误；生产构建包合成数据验收通过，真实登录后榜单目视仍待人工验收。本次未执行恢复演练；证据见 [部署与运维](../docs/部署与运维.md)。

- 后续整数显示修正通过 PR #63 合并为 `main@babd013`，main CI `34453945966` 的 31 项单元测试、23 项隔离集成测试、构建和审计均通过。新一轮双备份完成 OSS CRC64 校验后 app-only 更新成功；版本、镜像、151 文件静态树、7 个公网资源、迁移、健康、鉴权、跳转、安全头、timer 和日志全部通过，回滚源码与镜像保留。生产构建包合成数据实时榜和快照显示 `90`、`89`、`90.25` 与 `123.46兆`，正式浏览器跳转登录页；真实登录后目视与本次恢复演练未执行。详见 [部署与运维](../docs/部署与运维.md)。

## 2026-09-10 [Codex] 玩家筛选与排行榜整榜清空

- 玩家管理在搜索框右侧新增原生状态下拉框，可筛选全部、正常、物品封禁、存档封禁和榜单封禁；筛选写入 URL，切换时回到第一页并清空当前选择，当前视图摘要同步展示状态。
- 玩家列表 API 新增 `banStatus=normal|item|data|rank`，与关键词、排序和分页共同在当前地图内执行；正常表示三类封禁均关闭，封禁类型可重叠命中，非法值返回 400，无数据库迁移和权限变化。
- AI 文档治理检查、30 项单元测试和生产构建通过；新增全链路集成测试覆盖全部状态、正常、三类封禁、重叠命中、关键词组合、分页总数和非法参数。本机没有 PostgreSQL/Docker 或隔离测试库监听端口，集成测试未执行。
- 浏览器使用明确标注的本地合成数据验证桌面和 390px 窄屏布局、正常与榜单封禁切换、URL、摘要、结果和控制台；恢复桌面视口后保留预览标签页。
- 排行榜实时候选视图新增“清空实时榜”危险操作，可一次删除当前榜单全部候选记录，包括未显示和因榜单封禁隐藏的条目；服务端要求 `leaderboards.manage` 与 `confirm=true`，事务内校验地图和榜单归属并写入 `leaderboard.entries.clear` 审计。排行榜配置、历史快照和每日采集事实保留，实时策略下玩家再次上报仍可重新入榜。
- 整榜清空功能通过文档治理检查、30 项单元测试、生产构建，以及桌面和 390px 窄屏合成数据预览；确认框明确展示删除范围与保留数据，控制台无报错。本机仍无隔离 PostgreSQL，新增的权限、确认、地图隔离、全量删除、保留快照和重报回榜集成测试未执行。
- 上述功能通过 PR #59 合并为 `main@d740db8`，main CI `34445000238` 的 required checks 均成功；已创建 `deploy-d740db8` 并完成广州正式 ECS 的 app-only 发布。发布前数据库和上传卷双备份均非空且通过私有 OSS CRC64 校验；Release 哈希、151 文件静态树、版本、迁移、封禁列、容器和公网健康、跳转、安全头、缺 Key 401、备份 timer、0 重启及新 app error/fatal 为 0 均通过，旧镜像与源码已保留。正式浏览器加载并跳转登录页正常，未在正式数据上执行整榜清空；登录态交互和真实游戏上报仍待人工验收。

## 2026-09-09 [Codex] 排行榜修复与正式发布

- 实时榜增加复选框、全选当前显示和批量移除确认，一批最多 100 条。切换榜单、地图、搜索或快照清空选择；失败保留选择并持续显示错误，处理期间禁用重复操作，复选框点击区域至少 44px。
- 新增 POST /api/maps/:mapId/leaderboards/:leaderboardId/entries/batch-delete，要求 leaderboards.manage、唯一条目 ID 数组与 confirm=true。事务内锁定并核验全部条目归属，删除与审计共同提交，缺失或跨榜跨地图整批拒绝；历史快照和每日采集事实不变。
- npm run check 通过（30 项单元测试及构建），独立临时 PostgreSQL fengqi_test 库内 21 项集成测试通过，生产依赖审计 0 漏洞。新增验证覆盖确认字段、数量和重复校验、权限、归属、整批回滚、审计、快照及当日重报；临时测试库已清理。
- 浏览器在 localhost:4174 合成数据预览验证单选/全选、取消、删除后空状态、失败重试、搜索清空选择、快照只读和查看权限隐藏；390px 窄屏工具栏与选择列可达，控制台无警告/错误。已恢复桌面并保留预览标签；没有删除业务排行榜记录。
- 排行榜上限、批量移除、multer 与首次启动修复通过 PR #58 合并为 `main@378d3fe`，目标 main CI `34365347826` 两项检查成功；从干净提交构建并上传 `deploy-378d3fe`，2026-09-09 已完成正式 app-only 发布。双备份与 OSS CRC64、镜像及版本、迁移、健康、HTTPS/鉴权/跳转、安全头和静态资源通过，旧源码和镜像保留。首次下载超时未影响线上，断点续传后成功；详见 [部署与运维](../docs/部署与运维.md)。
- 刀塔客户端 JSON 保留整数与 17 位浮点有效数字、长分片搜索上界优化已提交并推送 `39116a8d`；编码和预加载分片测试通过，其他用户改动保留。按用户要求不处理地图包，真实地图上报与多人预加载仍待发布后验证；网站上线不会自动更新游戏平台地图。

## 2026-09-09 [Codex] multer 上传安全修复

- multer 从 2.2.0 升级至 2.3.0 并更新 npm 锁文件；上传文本字段不使用数组索引，设置 fieldArrayIndexLimit=0，复用原有 MulterError 的 400 响应。
- 上传全链路补充正数组索引与超大文件拒绝验证，确认拒绝请求没有文件元数据写入；原有正常上传下载、危险扩展名拒绝与目录级联删除均通过。
- Node 24 容器内 npm run check 通过（30 项单元测试及生产构建），独立临时 PostgreSQL fengqi_test 库内 20 项集成测试通过（UPLOAD_MAX_MB=1），npm run audit:prod 为 0 漏洞。
- Docker 再次遇到遗留 socket 启动故障，备份运行时目录后恢复。本地更新曾返回 Compose 临时容器名冲突，复核时旧容器已消失；运行镜像与 latest 一致，multer 实际版本 2.3.0，健康接口 status=ok。未执行正式发布或 Git 提交；此前排行榜上限和启动修复改动保留。
