# 部署与恢复

更新时间：2026-07-14

操作步骤以 `docs/部署与运维.md` 为准，本文件记录开发不能破坏的部署合约。

## 生产拓扑

- Compose 管理 PostgreSQL、应用和 Caddy；数据库使用命名卷且不发布公网端口。
- Caddy 使用 `SITE_ADDRESS` 申请/续期证书并反向代理应用；正式站点必须 HTTPS。
- 应用启动先等待数据库健康，再按顺序执行迁移；仅当数据库完全没有用户时创建初始管理员。
- `.env` 由首次配置生成且不进入 Git，数据库密码与 `DATABASE_URL` 必须一致。

## 原生模式

- 当 Docker 不可用时，本机脚本创建最小权限 PostgreSQL 角色并启动隐藏 Node 进程。
- 只监听 `127.0.0.1:3000`；无 Caddy 自动 HTTPS，不允许直接暴露公网。
- 管理员明文密码和 PostgreSQL 管理员密码不得保留在生成的 `.env`。

## 备份与恢复

- PostgreSQL 使用自定义格式备份并按保留期清理；恢复要求输入目标数据库名确认，并由脚本强制生成恢复前安全备份。
- 数据库恢复先停止应用写入；恢复失败时应用保持停止，恢复成功后必须等待应用健康检查通过。
- 上传卷不在数据库备份中；`备份上传文件.ps1` 与 `恢复上传文件.ps1` 提供独立快照、路径安全校验、恢复前安全快照和恢复后健康检查。
- 两类备份脚本支持可选 `-PassThru` 返回产物；`备份到OSS.ps1` 负责上传到私有 OSS `prod/YYYY/MM/DD/`、请求 AES-256 加密、比较本地与远端 CRC64，并在失败时调用服务器 `msmtp` 告警。
- Linux 正式调度模板位于 `运维/systemd/`，固定每天 `19:30 UTC`（北京时间 03:30）运行并启用持久补跑；OSS 与邮件凭据只允许存在于服务器 root 可读配置文件。
- 本机 `backups/` 不是异机备份；正式环境必须另行配置异机或对象存储副本、调度、告警和负责人。

## 已完成的本机容器验收

- 2026-07-14 在启用固件虚拟化后安装并启动 Docker Desktop 4.82.0，Linux Engine 29.6.1 可用。
- 生产镜像在 Node 24/npm 11.16 环境完成 `npm ci` 与前端构建；Compose 的 PostgreSQL、应用和 Caddy 均成功启动，数据库与应用健康。
- 通过本机 HTTP Caddy 验证健康接口、安全响应头、管理员与普通用户授权边界；浏览器首页、登录页和管理员工作台正常渲染，控制台无警告或错误。
- 隔离项目完成数据库双备份恢复与上传卷快照恢复；验收容器、卷、临时 `.env`、备份和测试上传数据已销毁。

## 远程代码检查

- 代码托管于私有 GitHub 仓库，`main` 已跟踪 `origin/main`。
- `.github/workflows/ci.yml` 使用 Node 24 与 PostgreSQL 17，在推送、Pull Request 和手工触发时执行 `npm run check`、`npm run audit:prod` 和隔离集成测试。
- 2026-07-15 提交 `daa9063` 的两个 CI 作业均成功；当前 GitHub 套餐不支持该私有仓库的 branch protection/rulesets，不能宣称 required checks 已强制启用。

## 当前未完成验收

- 正式域名确定为 `fengqigame.com`；2026-07-15 阿里云显示“注册局审核中”，现有 8 条记录均为企业邮箱 MX/SPF/CNAME，没有网站 A/CNAME，公网尚未解析。
- 阿里云账号当前没有轻量应用服务器实例；既有 OSS Bucket `war3mapload` 属于其他业务，不能默认作为本系统备份目标。
- 用户确认尚未购买或指定本项目正式服务器；不得从本机旧 SSH 别名、历史 IP 或其他项目配置推断正式服务器。购买或明确指定可信实例前，不执行远程部署、防火墙或 HTTPS 配置。
- 正式 OSS Bucket 已创建并启用私有访问、阻止公共访问、14 天对象过期和 1 天分片清理；RAM 最小权限用户与策略已绑定。Bucket 默认加密仍需 API 查询确认，AccessKey 尚未创建；正式 timer、邮件告警和远端恢复须等待正式服务器。
- 本机容器与隔离恢复通过不能替代真实域名证书和正式基础设施验收。

主要证据：`docker-compose.yml`、`Dockerfile`、`Caddyfile`、`首次配置.ps1`、`启动网站.bat`、`运维/`。
