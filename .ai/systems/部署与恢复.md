# 部署与恢复

更新时间：2026-08-25

操作步骤以 `docs/部署与运维.md` 为准，本文件记录开发不能破坏的部署合约。

## 生产拓扑

- Compose 管理 PostgreSQL、应用和 Caddy；数据库使用命名卷且不发布公网端口。
- Caddy 使用 `SITE_ADDRESS` 申请/续期主站证书并反向代理应用；`SITE_ALIAS_ADDRESS` 可为别名申请证书并永久跳转到主站，正式站点必须 HTTPS。
- 应用启动先等待数据库健康，再按顺序执行迁移；仅当数据库完全没有用户时创建初始管理员。
- `.env` 由首次配置生成且不进入 Git，数据库密码与 `DATABASE_URL` 必须一致。
- 日常远程发布使用本机阿里云 CLI OAuth 和只绑定广州正式实例的云助手最小权限；不使用长期 AccessKey，SSH 只保留固定 `/32` 的应急恢复边界。

## 原生模式

- 当 Docker 不可用时，本机脚本创建最小权限 PostgreSQL 角色并启动隐藏 Node 进程。
- 只监听 `127.0.0.1:3000`；无 Caddy 自动 HTTPS，不允许直接暴露公网。
- 管理员明文密码和 PostgreSQL 管理员密码不得保留在生成的 `.env`。

## 备份与恢复

- PostgreSQL 使用自定义格式备份并按保留期清理；恢复要求输入目标数据库名确认，并由脚本强制生成恢复前安全备份。
- 数据库恢复先停止应用写入；恢复失败时应用保持停止，恢复成功后必须等待应用健康检查通过。
- 上传卷不在数据库备份中；`备份上传文件.ps1` 与 `恢复上传文件.ps1` 提供独立快照、路径安全校验、恢复前安全快照和恢复后健康检查。
- 两类备份脚本支持可选 `-PassThru` 返回产物；`备份到OSS.ps1` 负责上传到私有 OSS `prod/YYYY/MM/DD/`、请求 AES-256 加密、比较本地与远端 CRC64，并在失败时调用服务器 `msmtp` 告警。
- Linux 正式调度模板位于 `运维/systemd/`，固定每天 `19:30 UTC`（北京时间 03:30）运行并启用持久补跑；OSS 与邮件凭据只允许存在于服务器 root 可读配置文件。
- 本机 `backups/` 不是异机备份；正式环境必须配置异机或对象存储副本、调度、告警和负责人，并以实际恢复演练验收。

## 已完成的本机容器验收

- 2026-07-14 在启用固件虚拟化后安装并启动 Docker Desktop 4.82.0，Linux Engine 29.6.1 可用。
- 生产镜像在 Node 24/npm 11.16 环境完成 `npm ci` 与前端构建；Compose 的 PostgreSQL、应用和 Caddy 均成功启动，数据库与应用健康。
- 通过本机 HTTP Caddy 验证健康接口、安全响应头、管理员与普通用户授权边界；浏览器首页、登录页和管理员工作台正常渲染，控制台无警告或错误。
- 隔离项目完成数据库双备份恢复与上传卷快照恢复；验收容器、卷、临时 `.env`、备份和测试上传数据已销毁。

## 远程代码检查

- 代码托管于公有 GitHub 仓库，`main` 已跟踪 `origin/main`。
- `.github/workflows/ci.yml` 使用 Node 24 与 PostgreSQL 17，在推送、Pull Request 和手工触发时执行 `npm run check`、`npm run audit:prod` 和隔离集成测试。
- 2026-07-15 发布提交 `09ace64` 的 `main` CI 运行 `29401624116` 成功；`main` 已强制要求质量/审计与隔离集成测试成功且分支保持最新，管理员不能绕过，强推和删除均被禁止。

## 已完成的正式环境验收

- 广州 ECS `8.148.249.198` 运行 Ubuntu 24.04 与正式 Compose；PostgreSQL 和应用健康，Caddy 只发布 80/443 TCP 与 443 UDP，数据库和应用端口不映射公网。
- 2026-07-20 已部署应用源码提交 `b8f17d9` 的备案后上线候选镜像 `sha256:ce93d8eb495fc9e01eeaea33ec70a9c88ad99c7689a0a85a0f876f868aa8a3ba`；发布前正式两类备份均完成 OSS 校验，更新只重建 app，没有改变 `.env`，也没有删除或重建 PostgreSQL、上传文件和 Caddy 数据卷。
- 2026-07-21 已部署合并提交 `4d47924` 的 FQ 批量协议镜像 `sha256:3dc8cd9531e0594d400701422be642541c7f21dc51a525ccd663b843e9f19466`；发布前两类备份完成 OSS 校验，更新仍只重建 app，并保留旧镜像回滚标签 `pre-4d47924`。
- 2026-07-22 已部署 `main@803f9ee` 的礼包实时资格版本，正式 app 镜像为 `sha256:2eff740a382dd256e8363cdd41272ea2f3eaf3876772c055c46213cb525ca3b6`；发布前数据库与上传卷备份均完成 OSS 校验，`007_gift_entitlements.sql` 已应用，`.env`、PostgreSQL、Caddy 和正式业务卷未改变，旧镜像与源码包已保留。
- 2026-07-22 已部署 `main@7f7bba5` 的礼包完整昵称识别版本，正式 app 镜像为 `sha256:b1c835f73179223978b97e4c498c30b39a8f72f9ed669913b31d0eee6b216420`；发布前两类备份 `20260722-180122` 已完成 OSS 校验。正式机 Docker Hub 超时时，以旧生产镜像为基底只复制 CI 已验证的 `server/routes/game.js`，仍只替换 app；旧镜像保留为 `pre-7f7bba5`。
- 2026-07-26 已部署 `main@1c57b300` 的排行榜跨环境配置复用版本，正式 app 镜像为 `sha256:b0b0bc6127090f6c8ecb217ca53f5df6e27b760b8c526b69316f0f60f15e0e3a`；发布前两类备份 `20260726-191658` 已完成私有 OSS 校验。正式机无法访问 Docker Hub 与 npm 官方注册表时，使用旧生产镜像的 Node 24 基底，在一次性容器内通过可达镜像源按最新 lockfile 全新安装依赖、运行检查并生成正式构建，再替换完整 `/app`；生产依赖审计为 0，旧镜像与源码包分别保留为 `pre-1c57b300` 和 `source-pre-1c57b300.tar.gz`。
- 2026-07-28 已部署 `main@f2b2679` 的排行榜环境切换误报修复版本，正式 app 镜像为 `sha256:e8ff11cfaed470e750bf0d8199f473b0455dfbbb9c1ea1771907edaaf8cfeaec`；发布前数据库备份 `20260728-011629` 与上传卷备份 `20260728-011630` 已完成私有 OSS 校验。此次以旧正式镜像为基底只替换本机正式构建的 `/app/dist` 并重建 app；`PostgreSQL`、Caddy、`.env`、服务端代码和正式业务卷未改变，旧镜像与源码包分别保留为 `pre-f2b2679` 和 `source-pre-f2b2679.tar.gz`。
- 2026-08-05 已部署 `codex/fq-single-runtime-space@2a91da9` 的 FQ 单运行空间版本；PR #27 两项 CI 均通过。正式 Compose 改用 `fengqi-admin_postgres_single_data` 与 `fengqi-admin_uploads_single_data` 新空卷，旧 `fengqi-admin_postgres_data`、`fengqi-admin_uploads_data` 离线保留，Caddy 卷、`.env` 和既有 OSS 历史备份未改。本次按明确授权不新建备份、不迁移旧数据；现有备份脚本分别操作运行中的 `db` 服务和 `/app/uploads` 实际挂载，后续定时任务仍覆盖在线新卷。新基线、管理员、地图 ID 1/2、三榜、35 个礼包和两把最小权限 Key 已恢复，生产烟测清理后运行数据、会话和审计为 0。初始化密码已从运行配置和临时文件移除，维护 SSH 公钥及 UFW 临时规则已撤销。
- 2026-08-08 已通过云助手把 `main@bc344155` 发布为镜像 `sha256:5d497141e5df59a8a2e0b5081a6079e29fe26be4076a3961255e303b1584de2f`；发布前数据库与上传卷备份 `fengqi-20260808-021720.dump`、`fengqi-uploads-20260808-021720.tar.gz` 均完成私有 OSS CRC64 校验。发布只重建 app，迁移 `002_leaderboard_daily_collections.sql` 已登记，排行榜每日采集表存在，app/db healthy，备份 timer active，公网 HTTP 308、HTTPS 200、`www` 301、安全响应头与缺 Key 401 均通过；旧镜像标签 `pre-bc344155-20260807-181921` 和源码备份 `source-pre-bc344155-20260807-181921.tar.gz` 已保留。
- 2026-08-09 已通过云助手把 PR #30 的合并提交 `main@ce7cec1` 发布为镜像 `sha256:0f6430ddad7b9dbba67f1c927daef72bc9889d416b93a9c29bc1ad463f8d01e5`；`main` CI 运行 `31273132058` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260809-030040.dump`、`fengqi-uploads-20260809-030041.tar.gz` 均完成私有 OSS CRC64 校验；发布载荷的源码包与正式构建包完成 SHA-256 校验。首次镜像构建因仓库 `.dockerignore` 排除 `dist` 在同步源码前失败，自动回滚恢复旧镜像并通过健康检查；随后改用独立最小构建上下文完成 app-only 发布。`001`、`002` 迁移登记、`players.rank_ban`、排行榜每日采集表和快捷封禁路由均存在，app/db healthy、Caddy running、备份 timer active，公网 HTTP 308、HTTPS 200、`www` 301、安全响应头、健康接口 200、缺 Key 401 与新前端资源均通过；旧镜像标签 `pre-bc344155-20260809-030317` 和源码备份 `source-pre-bc344155-20260809-030317.tar.gz` 已保留。
- 2026-08-12 已通过云助手把 PR #32 的合并提交 `main@10f453c` 发布为镜像 `sha256:ee86044631f1c7ff7aee8226907772d9a4102be5caf5e7ceb8e18d23dd9b801a`；`main` CI 运行 `31612077849` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260812-232707.dump`、`fengqi-uploads-20260812-232708.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与正式构建包 SHA-256 与 `deploy-10f453c` 一致。发布以旧正式镜像为基底替换 `server` 与新鲜 `dist`，只重建 app；`001`、`002` 迁移登记、排行榜每日采集表与 `players.rank_ban` 均存在，app/db healthy、Caddy running、备份 timer active/enabled，公网 HTTP 308、HTTPS 200、`www` 301、安全响应头、健康接口 200、缺 Key 401 和前端入口资源哈希均通过，近 15 分钟 app error/fatal 为 0。旧镜像标签 `pre-ce7cec1-20260812-232910` 和源码备份 `source-pre-ce7cec1-20260812-232910.tar.gz` 已保留；Chrome 扩展侧连续两次超时，未完成正式页面的真实浏览器可见状态与控制台验收。
- 2026-08-17 已通过云助手把 PR #34 的合并提交 `main@82f614b` 发布为镜像 `sha256:77862f1739b56316190463a0f672ffeab3e9cfca3d45800a97e02ff65e88ce24`；`main` CI 运行 `31979948451` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260817-074840.dump`、`fengqi-uploads-20260817-074841.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与隔离目录生成的 145 文件正式构建包 SHA-256 与 `deploy-82f614b` 一致。发布以旧正式镜像为基底替换 `server` 与洁净 `dist`，只重建 app；`001`、`002` 迁移登记、排行榜每日采集表与 `players.rank_ban` 均存在，app/db healthy、Caddy running、备份 timer active/enabled，公网 HTTP 308、HTTPS 200、`www` 保留 URI 301、四项安全响应头、健康接口 200、缺 Key 401、前端 index/主 JS/CSS/AuthPage 资源哈希与近 15 分钟 app error/fatal 为 0 均通过。旧镜像标签 `pre-10f453c-20260817-075112`、校正前健康镜像标签 `pre-dist-cleanup-82f614b-20260817-075356` 和源码备份 `source-pre-10f453c-20260817-075112.tar.gz` 已保留；内置浏览器能导航并读取正式页标题，但 DOM/Canvas 与控制台读取连续超时，正式 WebGL 可见状态与控制台仍待人工补验。
- 2026-08-24 已通过云助手把 PR #37 的合并提交 `main@18f612c` 发布为镜像 `sha256:da23617d9be40b00388e1e51afc5de38c180e7e4993def28000d5eef1cf44b3a`；`main` CI 运行 `32740774916` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260824-224921.dump`、`fengqi-uploads-20260824-224922.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与 145 文件洁净构建包 SHA-256 与 `deploy-18f612c` 一致。发布只替换 app 与受版本控制源码；`001`、`002`、app/db healthy、Caddy running、备份 timer active/enabled，公网 HTTP 308、HTTPS 200、`www` 保留 URI 301、四项安全响应头、健康接口 200、缺 Key 401、前端 index/主 JS/CSS 与近 15 分钟 app error/fatal 为 0 均通过。旧镜像标签 `pre-82f614b-20260824-225124` 与源码备份 `source-pre-82f614b-20260824-225124.tar.gz` 已保留，数据库、上传卷、Caddy、`.env` 和网络未改变。地图 ID 2 预加载代码为空；正式登录后的配置页视觉状态和真实游戏客户端仍待验收。血液地图 Key 必须在新地图发布并确认 `includeMessages=false` 后再移除 `game.messages.read`。
- 2026-08-25 已通过云助手把 PR #39、#40 的最终合并提交 `main@54e0fb1` 发布为镜像 `sha256:1b9cf801bcf09ebc6f4b3a2c05b52cc1e52caf26fc34a02d3ed11b314bb237ac`；`main` CI 运行 `32863646149` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260825-230326.dump`、`fengqi-uploads-20260825-230327.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与 145 文件洁净构建包 SHA-256 与 `deploy-54e0fb1` 一致。正式机使用腾讯 npm 镜像安装 lockfile 的生产依赖，并以不含 `.dockerignore` 的独立最小上下文构建；镜像先清空旧 `server`、`shared`、`dist` 再复制载荷，最终静态树与公网 index、主 JS/CSS、ConfigPanel 哈希一致。`001`、`002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy running、备份 timer active/enabled、公网跳转、安全头、健康接口、缺 Key 401 与近 15 分钟日志均通过。旧镜像、校正前健康镜像和旧源码包已保留，数据库、上传卷、Caddy、`.env` 和网络未改变；正式登录后的配置页交互与真实游戏客户端仍待验收。
- 2026-08-26 已通过云助手把 PR #42 的合并提交 `main@c10ebd5` 发布为镜像 `sha256:3a45b0d975a7f0c801bba6ebd6776390fd6752f1fed4f6f932219fdf1c936d5d`；`main` CI 运行 `32872981407` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260826-004133.dump`、`fengqi-uploads-20260826-004134.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与 145 文件洁净构建包 SHA-256 与 `deploy-c10ebd5` 一致。发布以旧正式镜像依赖层替换 `server`、`shared` 与洁净 `dist`，只重建 app；`001`、`002`、排行榜每日采集表、`players.rank_ban`、app/db healthy、Caddy running、备份 timer active/enabled、公网 HTTP 308、HTTPS 200、`www` 保留 URI 301、四项安全响应头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0，完整静态树与公网入口 JS/CSS 均和发布包一致。旧镜像标签 `pre-54e0fb1-20260826-005149` 与源码备份 `source-pre-54e0fb1-20260826-005149.tar.gz` 已保留，数据库、上传卷、Caddy、`.env` 和网络未改变；正式浏览器可读取站点标题，登录后的配置页交互与控制台仍待验收。
- 2026-08-26 已通过云助手把补强 PR #44 的合并提交 `main@ed5c76c` 发布为镜像 `sha256:7f807311864942f8af52a975afd838c934ce5b2df70d61ecc3625f422bd1dd16`；`main` CI 运行 `32941529810` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260826-151545.dump`、`fengqi-uploads-20260826-151545.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与 145 文件洁净构建包 SHA-256 与 `deploy-ed5c76c` 一致。第一次切换因源码复核包含根目录时间而误报漂移，自动回滚到 `c10ebd5` 且 app 恢复健康；改为忽略目录时间、继续逐文件校验后 app-only 发布成功。`001`、`002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy running、备份 timer active/enabled、公网 HTTP 308、HTTPS 200、`www` 保留 URI 301、安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0、完整静态树与 3 个公网入口资源均通过；生产镜像内预加载注释精简与 bootstrap 持久化读取烟测通过。旧镜像标签 `pre-c10ebd5-20260826-152351` 与源码备份 `source-pre-c10ebd5-20260826-152351.tar.gz` 已保留，数据库、上传卷、Caddy、`.env`、网络和正式预加载数据未改变；真实地图 MD5、编译、执行与多人同步仍待联调。
- 2026-08-26 已通过云助手把 PR #46 的合并提交 `main@d8c7d7f` 发布为镜像 `sha256:b8df2d3f276bf30e43ea53e0c2cfcba1ee675b0a16717bdcf23e3a1917775e20`；`main` CI 运行 `32947345969` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260826-162752.dump`、`fengqi-uploads-20260826-162753.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与 145 文件洁净构建包 SHA-256 与 `deploy-d8c7d7f` 一致。app-only 发布后镜像 revision、完整文件树、`001/002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy running、备份 timer active/enabled、公网 HTTP 308、HTTPS 200、`www` 保留 URI 301、四项安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0 和 3 个公网入口资源均通过；生产镜像内被注释引用归空与动态 `require` 全工作区兼容烟测通过。旧镜像标签 `pre-ed5c76c-20260826-162943` 与源码备份 `source-pre-ed5c76c-20260826-162943.tar.gz` 已保留，数据库、上传卷、Caddy、`.env`、网络和正式预加载数据未改变；正式登录态配置页及真实地图 MD5、编译、执行与多人同步仍待联调。
- 2026-08-26 已通过云助手把 PR #48 的合并提交 `main@699b288` 发布为镜像 `sha256:19cc79ee047ad406104decc66463e9a2083955965ca4c29bd4e0e167e4640754`；`main` CI 运行 `32959725908` 的两项 required checks 均成功。发布前数据库与上传卷备份 `fengqi-20260826-185846.dump`、`fengqi-uploads-20260826-185846.tar.gz` 均完成私有 OSS CRC64 校验，服务器下载的源码包与 145 文件洁净构建包 SHA-256 与 `deploy-699b288` 一致。app-only 发布后镜像 revision、完整文件树、`001/002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy running、备份 timer active/enabled、公网 HTTP 308、HTTPS 200、`www` 保留 URI 301、四项安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0 及公网 index、主 JS/CSS、ConfigPanel 哈希均通过；生产镜像内未加载文件跳过语法问题与动态 `require` 全工作区兼容烟测通过。旧镜像标签 `pre-d8c7d7f-20260826-190000` 与源码备份 `source-pre-d8c7d7f-20260826-190000.tar.gz` 已保留，数据库、上传卷、Caddy、`.env`、网络和正式预加载数据未改变；正式登录态配置页及真实地图 MD5、编译、执行与多人同步仍待联调。
- Caddy 已取得 `fengqigame.com` 的有效 Let's Encrypt 证书；ICP 网站备案、网站 A 记录、公网 HTTP 308、HTTPS 200、浏览器证书和 `/api/system/health` 均已验证。`www.fengqigame.com` 的 CNAME 已指向主站，Caddy 使用独立别名地址申请证书并将请求永久跳转到裸域名。
- 阿里云安全组与 UFW 均允许 80/443 公网访问；SSH 22 仅允许已确认管理员公网地址 `/32`，root、密码登录和未知主机密钥均不放行。
- 正式私有 OSS Bucket 使用武汉 LRS、阻止公共访问、14 天对象过期和 1 天分片清理；`HeadObject` 确认两类对象均为 SSE-OSS `AES256`。
- RAM 上传身份只允许 `prod/` 的列举、上传与读取校验；删除、ACL 修改、其他 Bucket 和匿名读取均被拒绝。
- systemd timer 已启用，每天北京时间 03:30 持久补跑；数据库与上传卷副本通过 CRC64 校验，QQ 告警负责人收到人工测试邮件。
- 最新 OSS 数据库与上传卷副本已在独立网络和独立卷实际恢复，恢复库包含 26 张业务表且隔离应用达到 healthy；随后销毁临时容器、网络、卷和下载文件。

主要证据：`docker-compose.yml`、`Dockerfile`、`Caddyfile`、`首次配置.ps1`、`启动网站.bat`、`运维/`。
