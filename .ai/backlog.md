# 已验证待办

只记录有源码、测试或运维清单证据的事项。状态使用 `待处理`、`进行中`、`已完成`、`阻塞`。

| 优先级 | 状态   | 事项                               | 证据                                                                                                                  | 验收条件                                                                           |
| ------ | ------ | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| P0     | 阻塞   | 正式服务器、Caddy 与 HTTPS 验收    | 正式域名 `fengqigame.com` 处于注册局审核中且没有网站 A/CNAME；新 SSH 指纹已由用户确认，但服务器连续两次未返回主机密钥 | 域名审核通过并解析到可信服务器，SSH 恢复后部署 Compose、健康检查通过并收紧公网端口 |
| P0     | 阻塞   | 正式备份调度、异机副本与告警       | 已完成 OSS 上传编排、最小权限策略与 systemd 模板，本机两类快照非空；Edge 创建按钮超时，服务器 TCP 22 通但 SSH 主机密钥不返回 | 私有 Bucket、RAM、timer 与失败邮件实际生效，远端副本在隔离环境恢复成功             |
| P1     | 已完成 | 远程 Git 与 CI 检查                | 私有 GitHub `origin` 已受控；CI 提交 `daa9063` 的质量与隔离集成测试作业均成功                                         | 远程仓库受控，CI 执行 `npm run check`、依赖审计和隔离集成测试                      |
| P1     | 阻塞   | `main` 分支强制 required checks    | GitHub API 返回当前私有仓库套餐不支持 branch protection/rulesets                                                      | 升级支持私有仓库保护的套餐或在不泄露源码的前提下迁移到支持该能力的平台             |
| P2     | 待处理 | 评估地图工作台和地图路由的模块边界 | `src/pages/MapWorkspace.jsx`、`server/routes/maps.js` 已承载多个业务域                                                | 先形成不改变 API 的拆分方案，再按业务域降低单文件维护风险                          |
