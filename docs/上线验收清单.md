# 上线验收清单

## 基础设施

- [x] 正式域名 DNS 已恢复；阿里云权威 DNS、223.5.5.5 与 1.1.1.1 均解析到正式 ECS，`www` CNAME 指向主站，公网 HTTP 308、HTTPS 证书、健康接口和备案号已验证
- [x] 公网业务端口只开放 80/443，SSH 仅允许管理员 `/32`，PostgreSQL 没有公网端口
- [x] `.env` 仅 root 可读，初始化管理员明文已移除，管理员和数据库使用独立密码
- [x] `docker compose ps` 三个服务均为 healthy/running
- [x] `/api/system/health` 返回 `status: ok`

## 账号与权限

以下生产写入项不得污染正式数据；对应管理员、受限用户、会话失效、密码重置与地图级最小权限须先由隔离 PostgreSQL 全链路和候选镜像浏览器验收覆盖。

- [ ] 初始管理员已登录并立即确认个人资料
- [ ] 创建一个普通用户，只授权一张测试地图和一个功能
- [ ] 普通用户看不到未授权地图、管理员菜单和未授权功能
- [ ] 停用普通用户后，原会话立即失效
- [ ] 重置密码后，该用户全部会话立即退出

## 地图与客户端链路

- [x] 正式 schema 无 `environment/runtime_env`；隔离集成测试确认同地图多 Key 共享数据、不同地图同 UID 仍互相隔离
- [ ] 客户端上报玩家、指标、日志和埋点后，后台数据正确
- [x] 两张地图的新 Key 分别使用 `/api/fq/bootstrap` 拉取专用测试玩家，返回正确 `mapId`、revision 0 空档且无环境字段
- [ ] 实际游戏客户端使用 `/api/fq/bootstrap` 拉取玩家与全局存档，确认封禁状态和全局 revision 正确
- [x] 玩家存档使用 `FQ-` requestId 保存；重复请求幂等，复用 ID 和旧 revision 均不会覆盖新数据
- [ ] 实际游戏客户端完成全局存档首次保存、重试与 revision 冲突验收
- [ ] 客户端一次批量读取全房消息与礼包资格；消息写入成功后 ACK，礼包每局覆盖且重复查询不累计
- [x] 每张地图使用独立最小权限 `FQ-Map-Key`；两把 Key 的真实活跃端点均已鉴权，越权 403 由隔离 PostgreSQL 全链路覆盖
- [ ] 停用任一 `FQ-Map-Key` 后，其请求立即返回 401

## 业务功能

- [ ] 地图配置保存后刷新页面仍一致，JSON 语法错误不能保存
- [ ] 玩家封禁状态、礼包、主播、埋点和日志增删改均写入审计日志
- [ ] 血液四人开局只由唯一主机发起 5 个批量 HTTP：bootstrap、资料、两个排行榜和礼包投递
- [ ] 只授予 `game.gifts.read` 的 Key 在 `includeMessages=false` 时可读取礼包，省略该字段时因缺少 `game.messages.read` 返回 403
- [ ] 同一 UID 同一北京时间自然日只接受首次榜单样本，并发重复提交只生效一次
- [ ] 未发布时游戏不显示候选池；人工发布后新开对局读取最新前100，历史快照不被后续数据改写
- [ ] 榜单封禁玩家不会进入实时榜和新快照，解除封禁后可重新参与排名
- [ ] 风险事件使用同一 `eventId` 重试不会重复入库，处置结果能联动三类玩家封禁
- [ ] 普通用户只有查看权限时不能创建榜单、规则或处置风险事件
- [ ] 群抽公开链接可报名，同一 UID 不能重复，开奖后显示中奖名单
- [ ] 文件上传、下载、文件夹和删除有效；超大文件及脚本文件被拒绝
- [x] 管理员运行数据清理要求输入地图名；生产烟测清理后地图定义、礼包、榜单和 Key 保留，运行数据归零
- [ ] 管理员永久删除地图经过两次风险确认，清除全部关联数据、API Key 与上传目录，同时保留审计和既有备份

## 恢复能力

- [x] 执行一次数据库备份并确认 `.dump` 文件非空
- [x] 在隔离 Docker 环境成功恢复数据库备份并核对回滚结果
- [x] 上传文件卷有独立快照与恢复脚本，并在隔离 Docker 卷完成回滚验证
- [x] 独立私有 OSS 已启用私有 ACL、阻止公共访问、14 天对象过期和 1 天分片清理；`HeadObject` 确认 AES-256
- [x] 最小权限 RAM 身份只能列举、上传和读取校验正式 Bucket 的 `prod/`；删除、ACL 修改和跨 Bucket 访问均被拒绝
- [x] 正式 systemd timer 每天北京时间 03:30 运行，数据库与上传卷异机副本 CRC64 一致
- [x] 失败告警测试邮件由已确认负责人收到，OSS 远端副本在隔离环境实际恢复成功

任何未勾选项都应记录负责人、计划时间和风险说明；未完成恢复演练前不建议承诺生产数据可恢复。

2026-08-05 单运行空间重建是经明确授权执行的无新备份、无旧数据迁移操作；当前线上只使用新空卷，旧数据库和上传卷仅离线保留，不作为本次失败回退路径。既有 OSS 历史备份与日常备份能力未删除。

## 当前自动化验收基线

- 单元测试覆盖密码与 Token、安全路径、PATCH 默认值、错误脱敏和非法 JSON。
- PostgreSQL 集成测试覆盖管理员、普通用户、FQ 游戏客户端、地图隔离、同地图多 Key、自动指标会话与 11 项公式、玩家/全局存档、revision 并发控制、requestId 幂等、封禁阻断、UID 迁移、消息 ACK、礼包实时资格、配置、主播、埋点、排行榜快照、风险事件与封禁联动、文件、抽奖、账号、审计和地图级清理。
- 本机原生模式已覆盖生产构建、迁移幂等、隐藏进程启动、受控重启、健康检查、静态资源、登录退出及备份恢复。
- 固件虚拟化开启后，已在本机 Docker Desktop 4.82.0 完成生产镜像构建、Compose 三服务启动、HTTP 反向代理、管理员/普通用户权限、数据库与上传卷恢复，以及浏览器首页/登录/工作台视觉检查；浏览器控制台无警告和错误。
- 正式域名 `fengqigame.com` 已完成公网验收：权威 DNS 与两个公共递归 DNS 均返回 `8.148.249.198`，HTTP 返回 308，HTTPS 健康接口返回 200；Let's Encrypt 证书域名正确且有效期至 2026-10-13，HSTS、CSP 和 ICP 网站备案号已验证。
- 正式 ECS 使用 Ubuntu 24.04、Docker Compose、Caddy 与双层防火墙；数据库和应用端口不发布公网，SSH 仅允许管理员 `/32`。
- 2026-07-20 应用源码提交 `b8f17d9` 的备案后上线候选已部署到正式 app，镜像为 `sha256:ce93d8eb495fc9e01eeaea33ec70a9c88ad99c7689a0a85a0f876f868aa8a3ba`；容器 healthy，`/api/fq` 未认证请求返回 `FQ_MISSING_API_KEY`，永久删除接口未登录返回 401，PostgreSQL、Caddy、`.env` 和正式空业务库均未改变。
- PR #8 已合并到 `main`，合并提交 `3d2df971` 的 CI 运行 `29732388086` 成功；质量审计和隔离 PostgreSQL 两项 required checks 均通过。
- PR #13 已合并为 `4d47924`，CI 运行 `29838537260` 的质量审计和 15 项隔离 PostgreSQL 全链路均通过。
- `4d47924` 已部署为镜像 `sha256:3dc8cd9531e0594d400701422be642541c7f21dc51a525ccd663b843e9f19466`；`006` 迁移、批量玩家/投递接口、发布榜查询、HTTPS 200 和 HTTP 308 均已验证，测试 UID 清理后残留为 0。
- PR #15 已合并为 `b6dfea7`，CI 运行 `29893842935` 的质量审计和 15 项隔离 PostgreSQL 全链路均通过；`main@803f9ee` 已部署为镜像 `sha256:2eff740a382dd256e8363cdd41272ea2f3eaf3876772c055c46213cb525ca3b6`。发布前两类备份 `20260722-133938` 已完成 OSS 校验，`007`、礼包默认值 `0`、新旧资格表状态、三个服务健康、HTTPS 200 和 HTTP 308 均已验证；桌面/窄屏登录后冒烟及游戏内 0→1→重开→0 仍待完成。
- PR #18 已合并为 `7f7bba5`，`main` CI 运行 `29910204402` 的质量审计和 15 项隔离 PostgreSQL 全链路均通过；正式 app 已更新为镜像 `sha256:b1c835f73179223978b97e4c498c30b39a8f72f9ed669913b31d0eee6b216420`。发布前两类备份 `20260722-180122` 已完成 OSS 校验；使用地图 `test` Key 临时上报新 UID 与完整昵称 `酒徒#8023` 后，正式 `/api/fq/deliveries/query` 返回 `测试10级=1`，临时玩家清理后残留为 0。仅剩游戏内技能激活人工验收。
- OSS、RAM 最小权限、AES-256、CRC64、生命周期、03:30 timer、QQ 邮件告警及隔离恢复已完成正式验收。
- 2026-08-05 已部署 `codex/fq-single-runtime-space@2a91da9`；PR #27 两项 CI 通过。新空库只登记 `001_single_map_baseline.sql`，地图 ID 1/2、三榜、35 个礼包和两把新 Key 已恢复；正式域名冒烟及地图级清理完成，玩家、存档、指标、榜单条目、快照、会话和审计残留均为 0。旧三环境验收仅保留历史意义，地图实机联调仍未完成。
- 2026-08-08 已通过单实例最小权限云助手部署 `main@bc344155`。发布前数据库备份 `fengqi-20260808-021720.dump` 与上传卷备份 `fengqi-uploads-20260808-021720.tar.gz` 已完成私有 OSS CRC64 校验；迁移 `002_leaderboard_daily_collections.sql` 与排行榜每日采集表存在，app/db healthy，备份 timer active。公网 HTTP 308、HTTPS 200、`www` 301、安全响应头及缺少 `FQ-Map-Key` 时的 401 均通过；旧镜像和源码包已保留，可回滚。
- 2026-08-09 已部署 PR #30 的合并提交 `main@ce7cec1`，`main` CI 运行 `31273132058` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260809-030040.dump` 与上传卷备份 `fengqi-uploads-20260809-030041.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:0f6430ddad7b9dbba67f1c927daef72bc9889d416b93a9c29bc1ad463f8d01e5`，旧镜像标签与源码备份已保留。`001`、`002` 迁移登记、`players.rank_ban`、每日采集表、排行榜快捷封禁路由、app/db healthy、Caddy、备份 timer、新前端资源、公网 HTTP 308/HTTPS 200/WWW 301、安全响应头、健康接口 200 与缺 Key 401 均通过；本次只替换 app，未改数据库、上传卷、Caddy、`.env` 或网络。
- 2026-08-12 已部署 PR #32 的合并提交 `main@10f453c`，`main` CI 运行 `31612077849` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260812-232707.dump` 与上传卷备份 `fengqi-uploads-20260812-232708.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:ee86044631f1c7ff7aee8226907772d9a4102be5caf5e7ceb8e18d23dd9b801a`，旧镜像标签与源码备份已保留。`001`、`002` 迁移登记、排行榜每日采集表、`players.rank_ban`、app/db healthy、Caddy、备份 timer、新前端资源哈希、公网 HTTP 308/HTTPS 200/WWW 301、安全响应头、健康接口 200 与缺 Key 401 均通过；本次只替换 app，未改数据库、上传卷、Caddy、`.env` 或网络。Chrome 扩展侧连续两次超时，正式页面的真实浏览器可见状态和控制台仍待补验。
- 2026-08-17 已部署 PR #34 的合并提交 `main@82f614b`，`main` CI 运行 `31979948451` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260817-074840.dump` 与上传卷备份 `fengqi-uploads-20260817-074841.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:77862f1739b56316190463a0f672ffeab3e9cfca3d45800a97e02ff65e88ce24`，旧镜像、校正前健康镜像与旧源码包均已保留。发布包在隔离输出目录重建为 145 个当前文件，服务器下载哈希与 `deploy-82f614b` 一致；`001`、`002` 迁移登记、排行榜每日采集表、`players.rank_ban`、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、四项安全响应头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0，且 index、主 JS/CSS 与 AuthPage 资源哈希均通过。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env` 或网络。内置浏览器能导航至正式登录页并读取标题，但 DOM/Canvas 与控制台读取连续超时，正式 WebGL 可见状态和控制台仍待人工补验。
- 2026-08-24 已部署 PR #37 的合并提交 `main@18f612c`，`main` CI 运行 `32740774916` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260824-224921.dump` 与上传卷备份 `fengqi-uploads-20260824-224922.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:da23617d9be40b00388e1e51afc5de38c180e7e4993def28000d5eef1cf44b3a`，旧镜像与旧源码包均已保留。`deploy-18f612c` 的源码包和 145 文件洁净构建包哈希已在服务器复核；app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、四项安全响应头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0，且正式 index、主 JS/CSS 与本次构建一致。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env` 或网络。地图 ID 2 的预加载代码为空；正式登录后的 `0/256KB` 配置页视觉状态和真实游戏客户端仍待验收。血液地图 Key 在新地图发布并确认发送 `includeMessages=false` 前继续保留 `game.messages.read`。
- 2026-08-25 已部署 PR #39、#40 的最终合并提交 `main@54e0fb1`，`main` CI 运行 `32863646149` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260825-230326.dump` 与上传卷备份 `fengqi-uploads-20260825-230327.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:1b9cf801bcf09ebc6f4b3a2c05b52cc1e52caf26fc34a02d3ed11b314bb237ac`，旧镜像、校正前健康镜像与旧源码包均已保留。`deploy-54e0fb1` 的源码包和 145 文件洁净构建包哈希已在服务器复核；`001`、`002`、排行榜每日采集表、`players.rank_ban`、共享预加载模块、三容器 0 重启、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0，且完整静态树及公网 index、主 JS/CSS、ConfigPanel 与发布包一致。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env` 或网络。内置浏览器能打开正式登录页并读取标题“风起游戏”，但 DOM/Canvas 与控制台读取超时；正式登录后的配置页交互和真实游戏客户端仍待验收。
- 2026-08-26 已部署 PR #42 的合并提交 `main@c10ebd5`，`main` CI 运行 `32872981407` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260826-004133.dump` 与上传卷备份 `fengqi-uploads-20260826-004134.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:3a45b0d975a7f0c801bba6ebd6776390fd6752f1fed4f6f932219fdf1c936d5d`，旧镜像标签 `pre-54e0fb1-20260826-005149` 与旧源码包已保留。`deploy-c10ebd5` 的源码包和 145 文件洁净构建包哈希已在服务器复核；`001`、`002`、排行榜每日采集表、`players.rank_ban`、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、四项安全响应头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0，且完整静态树及公网入口 JS/CSS 与发布包一致。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env` 或网络。内置浏览器可读取标题“风起游戏”，但登录后的配置页 DOM/控制台读取超时；正式页面拖拽和游戏客户端执行仍待验收。
- 2026-08-26 已部署补强 PR #44 的合并提交 `main@ed5c76c`，`main` CI 运行 `32941529810` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260826-151545.dump` 与上传卷备份 `fengqi-uploads-20260826-151545.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:7f807311864942f8af52a975afd838c934ce5b2df70d61ecc3625f422bd1dd16`，旧镜像标签 `pre-c10ebd5-20260826-152351` 与旧源码包已保留。`deploy-ed5c76c` 的源码包和 145 文件洁净构建包哈希已在服务器复核；第一次切换因根目录时间误报源码漂移而自动回滚并恢复健康，修正复核门禁后发布成功。`001`、`002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0、完整静态树和 3 个公网入口资源均通过；生产镜像内纯注释归空、字符串内 `--` 保留及 bootstrap 直接读取持久化字段烟测通过。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env`、网络或正式预加载数据；正式登录态配置页和真实游戏客户端仍待验收。
- 2026-08-26 已部署 PR #46 的合并提交 `main@d8c7d7f`，`main` CI 运行 `32947345969` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260826-162752.dump` 与上传卷备份 `fengqi-uploads-20260826-162753.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:b8df2d3f276bf30e43ea53e0c2cfcba1ee675b0a16717bdcf23e3a1917775e20`，旧镜像标签 `pre-ed5c76c-20260826-162943` 与旧源码包已保留。`deploy-d8c7d7f` 的源码包和 145 文件洁净构建包哈希已在服务器复核；`001`、`002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、四项安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0、完整镜像文件树和 3 个公网入口资源均通过；生产镜像内被注释引用的文件归空、动态 `require` 保守打包全部文件烟测通过。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env`、网络或正式预加载数据；正式登录态配置页和真实游戏客户端仍待验收。
- 2026-08-26 已部署 PR #48 的合并提交 `main@699b288`，`main` CI 运行 `32959725908` 的两项 required checks 均成功。发布前数据库备份 `fengqi-20260826-185846.dump` 与上传卷备份 `fengqi-uploads-20260826-185846.tar.gz` 已完成私有 OSS CRC64 校验；正式 app 镜像为 `sha256:19cc79ee047ad406104decc66463e9a2083955965ca4c29bd4e0e167e4640754`，旧镜像标签 `pre-d8c7d7f-20260826-190000` 与旧源码包已保留。`deploy-699b288` 的源码包和 145 文件洁净构建包哈希已在服务器复核；`001`、`002`、排行榜每日采集表、`players.rank_ban`、三容器 0 重启、app/db healthy、Caddy、备份 timer、公网 HTTP 308/HTTPS 200/WWW 保留 URI 301、四项安全头、健康接口 200、缺 Key 401、近 15 分钟 error/fatal 为 0、完整镜像文件树及公网 index、主 JS/CSS、ConfigPanel 均通过；生产镜像内未加载文件跳过问题检查、动态 `require` 保守检查全部文件烟测通过。本次只替换 app 与受版本控制源码，未改数据库、上传卷、Caddy、`.env`、网络或正式预加载数据；正式登录态配置页和真实游戏客户端仍待验收。
