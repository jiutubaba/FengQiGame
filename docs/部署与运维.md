# 部署与运维

## 生产前提

- 一台可运行 Docker Compose 的 Windows 或 Linux 服务器。
- 正式域名 `fengqigame.com` 的注册局审核和 ICP 网站备案均已通过；网站备案号为 `鄂ICP备2024058688号-2`。网站 A 记录固定指向正式 ECS，企业邮箱 MX/SPF/CNAME 记录保持不变。
- 防火墙只对公网开放 80/443 业务端口；日常发布使用阿里云云助手，SSH 22 只保留管理员固定 `/32` 应急入口，PostgreSQL 不映射宿主机端口。
- `.env`、数据库备份和上传文件不进入 Git，也不通过聊天或工单明文传输。

当前正式环境为广州 ECS `8.148.249.198`、Ubuntu 24.04、4 vCPU/8 GiB/100 GiB、5 Mbps。SSH 使用 `ecs-user` 登录后再通过免密 `sudo` 执行运维，禁止猜测或启用 `root` 直登。ICP 网站备案已于 2026-07-20 确认通过；同日阿里云权威 DNS、223.5.5.5 与 1.1.1.1 均确认 `fengqigame.com` 解析到正式 ECS，公网 HTTP 返回 308，HTTPS 健康接口返回 200。证书由 Let's Encrypt 签发给 `fengqigame.com`，有效期至 2026-10-13，HSTS、CSP 与其他安全响应头已生效。2026-07-23 新增 `www.fengqigame.com` CNAME 和 Caddy 别名入口，Let's Encrypt 证书包含该域名且有效期至 2026-10-21；HTTPS 请求以 301 保留原 URI 永久跳转到裸域名。

当前正式版本为 2026-09-17 每图可选分析与难度通关率版本 `main@1e47b97`，完整提交 `1e47b97611e6348c4959187bf430861137a21a13`；[PR #66](https://github.com/jiutubaba/FengQiGame/pull/66) 已合并，`main` CI `35235596915` 两项 required checks 成功。复审未发现阻断问题；31 项单元测试、25 项隔离 PostgreSQL 集成测试、文档门禁、生产构建通过，生产依赖审计 0 漏洞。新增难度通关率、选项选择统计和阶段退出统计均默认关闭，管理员在各地图“项目配置 → 开放功能”按需开启，地图端独立接入。

发布载荷见 [deploy-1e47b97](https://github.com/jiutubaba/FengQiGame/releases/tag/deploy-1e47b97)。从干净提交与独立 lockfile 安装生成源码包和 152 文件前端包，源码、前端、运行时 Dockerfile、静态哈希清单的 SHA-256 分别为 `42b7fc19a2ff4733221c69ae5cd0168fe02f6dab6129e6e503949c6b36dde7eb`、`4ccdfb849ddbeeef33f5d0958e2e46f3033e187e0cff43fc0e943adcc08f2c53`、`c81e6ba38dfcc192da4c903a8ea801e720437e7a4ae19a7c3266a55fd1709c14`、`767691d6530ffd3eb9c1687c6f7a302b0e5087f03c8b3e7b2589ad3267c2c50b`。前三个载荷在服务器校验；静态树另由文件逐项 SHA-256 复核。当前 app 镜像 `sha256:b50bd9ad2d62e5042e96247b488e871444d767d4fd2c7ada3f7b2eb38d89fb41`，revision 与完整提交一致。

发布前数据库备份 `fengqi-20260917-224658.dump`（1,296,104 字节）与上传备份 `fengqi-uploads-20260917-224658.tar.gz`（89 字节）均非空并通过私有 OSS CRC64 校验。预检云助手调用 `t-gz06xcju8ekkf7k` 确认原版本 `babd013`、容器健康、备份 timer 正常且无其他运行任务；成功部署调用 `t-gz06xck3s374lc0` 在同一远端互斥锁内完成双备份、下载、切换与验收。原始命令因长度上限被拒绝且未执行，随后对同一已审阅脚本压缩传输并在远端核验 SHA-256 后执行。下载期间旧 app 保持健康。

回滚镜像 `fengqi-admin-app:pre-babd013-20260917-224657` 与源码 `/opt/fengqigame/.runtime/source-pre-babd013-20260917-224657.tar.gz` 已保留；发布验收目录 `/opt/fengqigame/.runtime/deploy-1e47b97-20260917-224657`。lockfile 与旧镜像一致，复用原依赖层替换 server/shared/dist，仅重建 app；数据库和 Caddy 容器 ID、上传挂载、环境配置及网络均未改变。迁移 006/007 为前向追加，旧 app 可继续运行于增加后的表结构；失败自动恢复旧源码与镜像，本次未触发回滚或数据库恢复。

上线验收通过：001～007 迁移登记、四张分析表和地图开关字段/约束、app/db healthy、Caddy running、三容器 0 重启、备份 timer active/enabled、容器内与 HTTPS 健康 200、HTTP 308、www 保留 URI 301、四项安全头、新分析上报缺 Key 401/FQ_MISSING_API_KEY、后台分析未登录 401、152 文件完整静态树与 8 个公网入口/分析/配置资源一致，新 app error/fatal 为 0。正式浏览器可跳转登录页，但自动读取 DOM 连续两次超时，停止重试；正式登录后功能开关与数据页目视尚待人工验收。本地真实 API 和合成数据已验证双图/七列、开关隐藏与重开历史、筛选空状态和 390px 窄屏。未读取正式玩家资料或凭据，未进行真实地图上报或本次备份恢复演练。

刀塔客户端大数 JSON 编码与预加载分片性能修复已提交并推送至客户端仓库 `master@39116a8d20c437c913b751ac32784e6b2cfe1d57`。本次按用户要求不构建或发布地图包；游戏端修复需要重新打包并上传游戏平台后才会进入玩家使用的地图，后台发布不会替换已发布地图包。

地图 ID 2 当前旧格式 `preloadCode` 为 56,733 字节，尚未保存结构化 `preloadWorkspace`；新管理页读取时会将旧代码映射为根目录 `main.lua`。首次保存后，直接静态 `require("文件名.lua")` 只打包从入口可达的文件，被注释引用和未引用文件继续保留在源码工作区但不进入最大 256 KiB 的客户端 `preloadCode`；入口可达的动态或间接 `require` 会保守打包全部文件。正式登录后的配置页交互与真实游戏客户端仍待验收。已发布旧地图省略 `includeMessages`，服务端会按兼容默认值 `true` 处理，因此血液地图 Key 暂时保留 `game.messages.read`；必须先发布固定发送 `includeMessages=false` 的新地图并验证正式请求，再移除该权限。

2026-07-20 的 FQ 正式冒烟归档已通过 PR #9 合并；合并提交 `ed542807efac5062ae81342fa25021d9c4ed7b87` 的 `main` CI 运行 `29778462265` 成功。跨《沧澜》游戏项目继续维护时，先读 [沧澜项目 AI 技术交接](沧澜项目AI技术交接.md)。

服务器专属游戏接口统一使用 `/api/fq` 和 `FQ-Map-Key`，不保留 `/api/game`、`X-Map-Key`、环境字段或旧协议分支。正式库已登记 `001`～`007` 迁移，全部业务仅按 `map_id` 隔离；同地图可为权限拆分和轮换创建多把 Key，但地图发布配置只保存当前地图的一把最小权限 Key。

正式 Compose 当前挂载 `fengqi-admin_postgres_single_data` 与 `fengqi-admin_uploads_single_data`。旧 `fengqi-admin_postgres_data`、`fengqi-admin_uploads_data` 离线保留且无容器挂载；Caddy 数据/配置卷、`.env` 和既有 OSS 历史备份未改。本次按用户明确授权不创建新备份、不迁移旧数据，失败只向前修复新空实例，不把旧三环境卷恢复为线上数据源。现有数据库备份脚本操作运行中的 `db` 服务，上传备份脚本按 `/app/uploads` 实际挂载动态识别卷，因此后续定时任务会覆盖当前新卷；本次重建未手工触发新备份。

《沧澜》和《血液锦标赛2》的新 Key 已分别完成空档 bootstrap；生产烟测还覆盖存档、榜单和血液指标开始/心跳/结束。烟测后执行地图级运行数据清理，并清空初始化会话与审计，最终玩家、存档、指标、榜单条目、快照、会话和审计均为 0。管理员明文密码已从 `.env` 和临时文件移除；维护 SSH 公钥与 UFW 临时规则已撤销。地图源码已写入新 Key，但重新构建、发布和游戏内联调仍由用户手工完成。

2026-07-21 新协议部署后通过正式域名确认批量玩家 upsert、bootstrap、批量投递查询和 `landing_power` 发布快照查询可用；专用测试 UID 已精确删除且残留为 0。`006` 已登记，`last_submitted_on` 与 `achieved_at` 字段存在，HTTPS 健康接口返回 200、HTTP 返回 308，三个 Compose 服务保持运行。

2026-07-22 礼包实时资格版本部署后确认 `007` 已登记、`gift_entitlements` 存在、旧 `gift_grants` 已移除且礼包默认值为 `0`；当前迁移资格数为 `0`。公网 HTTPS 返回 200、HTTP 返回 308，app/db/caddy 均健康，新前端静态资源已生效；浏览器控制插件缺少运行文件，桌面与窄屏登录后实际冒烟仍待人工完成。

2026-07-22 完整昵称识别版本部署后，使用地图现有 `test` Key 通过正式域名临时上报新 UID、完整昵称 `酒徒#8023`，随后调用 `/api/fq/deliveries/query` 成功返回 `测试10级=1`，证明礼包资格可跨 UID 按完整昵称命中；临时玩家记录已精确删除且残留为 `0`，原昵称资格未改动。游戏内技能激活只剩人工开图验收。

## 首次部署

Windows 运行 `首次配置.ps1`，Linux 可复制 `.env.example` 为 `.env` 并手工填写。正式环境必须满足：

- `SITE_ADDRESS=https://后台域名`
- `SITE_ALIAS_ADDRESS=https://别名域名`；无别名时可保留默认值，正式站点的 `www` 别名应在 DNS 生效后配置
- `COOKIE_SECURE=true`
- `TRUST_PROXY=1`
- `ADMIN_PASSWORD` 至少 6 位且只用于本系统，不复用参考站密码
- `POSTGRES_PASSWORD` 与 `DATABASE_URL` 中的密码完全一致
- `API_KEY_ENCRYPTION_KEY` 为独立生成的 32 字节 Base64URL 密钥；不得与数据库密码复用，丢失后已保存的 API Key 密文无法解密

启动：

```powershell
docker compose up -d --build
docker compose ps
docker compose logs --tail 200 app
```

应用启动会在数据库健康后按文件名顺序运行尚未执行的迁移，并且仅在数据库完全没有用户时创建初始管理员。

## 无法重启时的本机原生模式

Windows 固件虚拟化暂未开启、Docker Desktop 不能运行时，`启动网站.bat` 会自动选择 `native`。该模式要求本机已安装并运行 PostgreSQL 与 Node.js：

- 首次配置只临时读取 PostgreSQL 管理员密码，用于创建 `fengqi` 数据库和无超级用户权限的 `fengqi` 角色；该密码不会写入 `.env`。
- 初始管理员由配置脚本直接写入数据库，管理员明文密码不会保留在原生模式 `.env`。
- 应用只监听 `http://127.0.0.1:3000`，运行日志位于 `.runtime/`，上传文件仍位于 `uploads/`。
- `运维/备份数据.ps1` 和 `运维/恢复数据.ps1` 会按 `DEPLOYMENT_MODE` 自动选择本机 PostgreSQL 工具或容器流程；原生恢复同样会先安全停止应用，成功后重新启动并检查健康状态。

原生模式没有 Caddy 自动 HTTPS，不能直接作为公网发布方式。电脑可以重启后，应在 BIOS 开启 SVM 并切换到 Docker 模式完成最终生产验收。

## 阿里云云助手发布通道

- 本机阿里云 CLI 使用 OAuth 配置 `fq-production`，每次发布均通过 `GetCallerIdentity` 实时核验身份，不能以配置名推断 RAM 用户。2026-09-17 由当前操作者授权 Codex 执行，实际返回 Account 身份；全程限定既有正式实例与应用发布操作，未修改云权限。不创建、不传递也不记录长期 AccessKey，OAuth Token 只保存在本机 CLI 配置中。
- `运维/阿里云云助手最小权限策略.json` 只允许广州正式实例 `i-7xvdufe80gxzqw3oowvo` 执行命令、查询结果和受控文件投递，不授予 ECS 启停/删除、网络、安全组或 RAM 管理能力。
- `运维/调用阿里云云助手.ps1` 会先确认目标实例 Agent 正常，再以 Base64 投递 Bash 脚本、轮询执行结果，并在云端返回失败或超时时终止本地发布流程。脚本内容不得输出 `.env`、Token、密码、生产玩家数据或备份内容。
- 发布载荷使用 GitHub 非正式发布 `deploy-<commit>` 的源码包和本机正式构建包；只包含 Git 已跟踪源码和 `dist`，上传前后核对 SHA-256，不包含 `.env`、备份、上传文件或 `.runtime`。
- 固定门禁为：两类 OSS 备份成功并校验 → 下载载荷并核对 SHA-256 → 保留旧源码和旧镜像 → 只替换 app → 检查迁移、容器和公网接口 → 失败自动恢复旧源码与旧镜像。数据库、上传卷、Caddy 和 `.env` 不随普通应用发布替换。

调用示例：

```powershell
$result = .\运维\调用阿里云云助手.ps1 -ScriptPath .\临时发布脚本.sh -TimeoutSeconds 1800
$result.Output
```

SSH 仅用于云助手不可用时的应急恢复；启用前仍须核对固定来源 `/32`、服务器主机指纹和回滚目标，完成后撤销临时放行。

## 更新

先备份，再拉取或覆盖新版本，然后执行：

```powershell
.\运维\备份数据.ps1
docker compose up -d --build
docker compose ps
```

正式 `/opt/fengqigame` 当前不是 Git 工作树。发布时从已通过 required CI 的提交生成 `git archive`，上传后核对 SHA-256，在临时目录解包，再用 `rsync --delete` 精确同步到正式目录；必须排除 `.env`、`backups/`、`uploads/` 和 `.runtime/`。若服务器无法访问 Docker Hub，允许在本机 Docker Desktop 构建 Linux/amd64 镜像，校验导出包后上传并 `docker load`，随后仅用 `docker compose up -d --no-deps --no-build app` 替换应用。

不要删除当前在线的 `postgres_single_data`、`uploads_single_data` 或 Caddy 数据卷；离线旧卷 `postgres_data`、`uploads_data` 也只能在用户另行明确授权后处理。`docker compose down` 不删除数据；`docker compose down -v` 会删除 Compose 当前声明的在线命名卷，只允许在明确销毁实例时使用。

## 代码托管与 CI

远程仓库为公有 GitHub `origin`。`.github/workflows/ci.yml` 会在 `main` 推送、Pull Request 和手工触发时运行：

- Node 24 下的 `npm run check`；
- `npm run audit:prod`；
- PostgreSQL 17 本机隔离测试库上的 `npm run test:integration`。

`main` 已启用分支保护：上述质量/审计作业和隔离 PostgreSQL 集成测试均为 required checks，分支必须保持最新，管理员同样不能绕过；强推和删除 `main` 均被禁止。当前未强制 Pull Request 审批，单人维护者仍可在两项检查成功后合并。

## 备份与恢复

手工备份：

```powershell
.\运维\备份数据.ps1 -RetentionDays 14
.\运维\备份上传文件.ps1 -RetentionDays 14
```

两条本机脚本都支持 `-PassThru` 返回生成的非空备份文件，供正式异机上传编排使用。正式 Linux 服务器通过 `运维/备份到OSS.ps1` 顺序生成数据库和上传卷快照，以 `PutObject` 和 `--server-side-encryption AES256` 上传到独立私有 OSS 的 `prod/YYYY/MM/DD/`，并比较本地与远端 CRC64；任一步骤失败都会以非零状态退出并尝试发送邮件告警。

正式备份使用独立的服务器配置，不复用应用 `.env`，也不把真实值写入 Git：

```text
FENGQI_OSS_BUCKET=fengqigame-prod-backups-cn-wuhan-20260715
FENGQI_OSS_CONFIG=/etc/fengqigame/ossutilconfig
FENGQI_BACKUP_ALERT_TO=<告警收件邮箱>
FENGQI_MSMTP_CONFIG=/etc/msmtprc
```

- Bucket 固定为标准存储、武汉 LRS、私有 ACL、阻止公共访问和 SSE-OSS AES-256；`prod/` 对象 14 天后删除，未完成分片 1 天后删除。
- RAM 用户只绑定 `运维/阿里云OSS最小权限策略.json`，只允许列举、上传和读取校验该 Bucket 的 `prod/` 前缀，不授予删除和权限管理能力。
- 使用 ossutil 2.x 交互式写入 `/etc/fengqigame/ossutilconfig`，Endpoint 为 `https://oss-cn-wuhan-lr.aliyuncs.com`；配置文件权限设为 `0600`。
- `msmtp` 使用 `smtp.qq.com:465` TLS；邮箱授权码单独保存在 root 可读的 `0600` 文件中，由部署者在服务器隐藏输入，不通过聊天、工单或仓库传输。

QQ SMTP 授权码使用隐藏提示录入：

```bash
pwsh -NoLogo -NoProfile -File /opt/fengqigame/运维/配置备份告警凭据.ps1
```

将仓库部署到 `/opt/fengqigame` 后安装并启用定时器：

```bash
install -d -m 0700 /etc/fengqigame /opt/fengqigame/backups
install -m 0644 运维/systemd/fengqigame-backup.service /etc/systemd/system/
install -m 0644 运维/systemd/fengqigame-backup.timer /etc/systemd/system/
systemctl daemon-reload
systemctl enable --now fengqigame-backup.timer
systemctl start fengqigame-backup.service
systemctl status fengqigame-backup.service
systemctl list-timers fengqigame-backup.timer
```

timer 每天 `19:30 UTC` 运行，即北京时间 03:30，并启用 `Persistent=true`。人工邮件验收使用 `备份到OSS.ps1 -AlertTest`，不得通过破坏正式备份任务制造失败。

数据库和上传文件是两套独立恢复对象。建议用 Windows 任务计划程序每天分别执行：

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "F:\风起游戏管理后台\运维\备份数据.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "F:\风起游戏管理后台\运维\备份上传文件.ps1"
```

数据库恢复命令会要求输入数据库名确认，随后自动生成恢复前安全备份、停止应用写入、执行恢复，并在应用重新通过健康检查后才报告成功：

```powershell
.\运维\恢复数据.ps1 -BackupPath .\backups\fengqi-YYYYMMDD-HHMMSS.dump
```

上传文件恢复同样要求明确确认并自动生成安全快照；归档中的绝对路径或 `..` 路径会被拒绝：

```powershell
.\运维\恢复上传文件.ps1 -SnapshotPath .\backups\fengqi-uploads-YYYYMMDD-HHMMSS.tar.gz
```

2026-07-15 已完成正式验收：timer 实际运行、两类 OSS 对象 CRC64 与 AES-256 通过、删除/ACL/跨 Bucket/匿名访问被拒绝、告警负责人收到测试邮件，最新数据库与上传卷副本在独立 Compose 项目成功恢复并通过健康检查。数据库与上传文件仍应定期重复隔离恢复演练。

## 监控和排障

- 健康检查：`GET /api/system/health`
- 容器状态：`docker compose ps`
- 应用日志：`docker compose logs -f --tail 200 app`
- 数据库日志：`docker compose logs -f --tail 200 db`
- 反向代理日志：`docker compose logs -f --tail 200 caddy`
- 管理员页面“系统设置”显示账号、地图、有效会话和 24 小时审计事件数量。

登录失败时先确认浏览器访问地址与 `SITE_ADDRESS` 一致；HTTPS 域名必须设置 `COOKIE_SECURE=true`。本机 HTTP 调试才允许使用 `COOKIE_SECURE=false`。
